---
title: React Native 설치하기
description: React Native 개발 환경을 처음부터 차근차근 설정하는 방법을 알아봅니다.
slug: react-native-installation
sidebar_position: 1
---

# React Native 설치 및 환경 설정

React Native로 앱을 개발하기 위해서는 개발 환경을 먼저 설정해야 합니다. 이 가이드에서는 초보자도 따라할 수 있도록 모든 과정을 상세히 설명하겠습니다.

## 두 가지 개발 방법 선택하기

React Native로 앱을 만드는 방법은 크게 두 가지가 있습니다.

### 1. Expo 사용하기 (초보자 강력 추천!)

**Expo**는 React Native 개발을 쉽게 시작할 수 있도록 도와주는 도구 모음입니다.

**장점**:
- 복잡한 설정이 거의 필요 없습니다
- 코드를 작성하면 바로 실제 폰에서 테스트할 수 있습니다
- 카메라, 위치, 센서 등 많은 기능이 이미 준비되어 있습니다
- Windows, Mac 모두에서 쉽게 시작할 수 있습니다

**단점**:
- 매우 특수한 네이티브 기능을 사용하려면 제약이 있을 수 있습니다
- 앱 크기가 약간 더 클 수 있습니다

**이런 분들에게 추천**:
- React Native를 처음 배우는 분
- 빠르게 프로토타입을 만들고 싶은 분
- 대부분의 일반적인 앱 기능만 필요한 분

### 2. React Native CLI 사용하기 (고급 사용자용)

**React Native CLI**는 더 많은 제어권을 주지만 설정이 복잡합니다.

**장점**:
- 모든 네이티브 코드에 직접 접근할 수 있습니다
- 네이티브 모듈을 자유롭게 수정할 수 있습니다
- 앱 크기와 성능을 더 세밀하게 최적화할 수 있습니다

**단점**:
- 초기 설정이 매우 복잡합니다
- Xcode(Mac), Android Studio 등 추가 도구 설치가 필요합니다
- 문제 해결이 어려울 수 있습니다

**이런 분들에게 추천**:
- 특수한 네이티브 기능이 필요한 분
- 네이티브 코드를 직접 작성할 줄 아는 분
- 앱 크기와 성능을 극한까지 최적화하려는 분

---

## 방법 1: Expo로 시작하기 (초보자 추천)

대부분의 초보자는 Expo로 시작하는 것을 강력히 추천합니다. 설정이 간단하고 바로 개발을 시작할 수 있습니다.

### 1단계: Node.js 설치하기

**Node.js**는 JavaScript를 컴퓨터에서 실행할 수 있게 해주는 프로그램입니다. React Native는 Node.js 위에서 동작합니다.

#### Windows에서 설치하기

1. [Node.js 공식 웹사이트](https://nodejs.org/)에 접속합니다
2. **LTS (Long Term Support)** 버전을 다운로드합니다
   - 2025년 기준으로 Node.js 22.x LTS 버전을 권장합니다
   - LTS는 "장기 지원" 버전으로 안정적입니다
3. 다운로드한 설치 파일을 실행합니다
4. 설치 마법사를 따라 진행합니다
   - **중요**: "Add to PATH" 옵션을 반드시 체크해주세요
   - 이 옵션을 체크하면 어디서든 Node.js 명령어를 사용할 수 있습니다

#### Mac에서 설치하기

**방법 1: 공식 웹사이트에서 설치 (쉬운 방법)**
1. [Node.js 공식 웹사이트](https://nodejs.org/)에 접속합니다
2. LTS 버전을 다운로드하고 설치합니다

**방법 2: Homebrew로 설치 (권장)**
1. 터미널(Terminal)을 엽니다
2. Homebrew가 없다면 먼저 설치합니다:
```bash
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
```

**코드 설명**:
- `/bin/bash`: bash 쉘을 실행합니다
- `-c`: 뒤따라오는 명령어를 실행합니다
- `curl -fsSL`: 인터넷에서 파일을 다운로드하는 명령어입니다
- Homebrew는 Mac에서 프로그램을 쉽게 설치할 수 있게 해주는 패키지 관리자입니다

3. Node.js를 설치합니다:
```bash
brew install node
```

#### 설치 확인하기

설치가 제대로 되었는지 확인해봅시다.

**Windows**: 명령 프롬프트(CMD) 또는 PowerShell을 엽니다
**Mac**: 터미널(Terminal)을 엽니다

다음 명령어를 입력합니다:

```bash
node --version
```

**출력 예시**: `v22.11.0` (버전 번호가 표시되면 성공입니다)

**코드 설명**:
- `node`: Node.js 프로그램을 실행합니다
- `--version` 또는 `-v`: 설치된 버전을 확인하는 옵션입니다

npm(Node Package Manager)도 함께 설치되었는지 확인합니다:

```bash
npm --version
```

**출력 예시**: `10.2.4` (버전 번호가 표시되면 성공입니다)

**npm이란?**
npm은 Node Package Manager의 약자로, JavaScript 라이브러리들을 쉽게 설치하고 관리할 수 있게 해주는 도구입니다. 마치 앱스토어처럼 필요한 라이브러리를 검색하고 설치할 수 있습니다.

### 2단계: Expo 프로젝트 만들기

이제 본격적으로 React Native 프로젝트를 만들어봅시다!

#### 터미널/명령 프롬프트 열기

**Windows**: 
- `Win + R` 키를 누르고 `cmd` 입력 후 엔터
- 또는 시작 메뉴에서 "명령 프롬프트" 또는 "PowerShell" 검색

**Mac**:
- `Command + Space` 키를 누르고 "terminal" 입력 후 엔터
- 또는 Spotlight 검색에서 "터미널" 검색

#### 프로젝트 생성하기

원하는 폴더로 이동한 후 (예: 문서 폴더나 바탕화면), 다음 명령어를 입력합니다:

```bash
npx create-expo-app my-first-app
```

**코드 한 줄씩 설명**:

- `npx`: npm 패키지를 임시로 실행하는 도구입니다
  - 프로그램을 컴퓨터에 설치하지 않고도 바로 실행할 수 있습니다
  - 항상 최신 버전을 사용할 수 있어서 좋습니다

- `create-expo-app`: Expo 프로젝트를 자동으로 생성해주는 도구입니다
  - 필요한 모든 파일과 설정을 자동으로 만들어줍니다

- `my-first-app`: 프로젝트의 이름입니다
  - 원하는 이름으로 변경할 수 있습니다
  - 공백은 사용할 수 없고, 하이픈(-) 또는 언더스코어(_)를 사용하세요
  - 예: `my-todo-app`, `awesome_app`, `HelloWorld`

**명령어 실행 후**:
- 필요한 파일들이 자동으로 다운로드되고 설치됩니다
- 몇 분 정도 걸릴 수 있으니 기다려주세요
- 완료되면 `✔ Your project is ready!` 같은 메시지가 나타납니다

### 3단계: 프로젝트 폴더로 이동하기

생성된 프로젝트 폴더로 이동합니다:

```bash
cd my-first-app
```

**코드 설명**:
- `cd`: "Change Directory"의 약자로, 폴더를 이동하는 명령어입니다
- `my-first-app`: 앞에서 만든 프로젝트 폴더 이름입니다

**현재 위치 확인하기**:
- Windows: `dir` 명령어로 현재 폴더의 파일 목록을 볼 수 있습니다
- Mac/Linux: `ls` 명령어로 현재 폴더의 파일 목록을 볼 수 있습니다

### 4단계: 개발 서버 시작하기

이제 앱을 실행해봅시다!

```bash
npx expo start
```

**코드 설명**:
- `npx expo start`: Expo 개발 서버를 시작하는 명령어입니다
  - Metro Bundler가 실행됩니다 (JavaScript 코드를 묶어주는 도구)
  - QR 코드가 터미널에 표시됩니다
  - 여러 가지 실행 옵션이 표시됩니다

**화면에 나타나는 내용**:
```
› Metro waiting on exp://192.168.0.1:8081
› Scan the QR code above with Expo Go (Android) or the Camera app (iOS)

› Press a │ open Android
› Press i │ open iOS simulator
› Press w │ open web

› Press r │ reload app
› Press m │ toggle menu
```

각 옵션의 의미:
- `a`: Android 에뮬레이터에서 실행
- `i`: iOS 시뮬레이터에서 실행 (Mac에서만 가능)
- `w`: 웹 브라우저에서 실행
- `r`: 앱을 다시 로드 (새로고침)
- `m`: 개발자 메뉴 열기

### 5단계: 스마트폰에서 앱 실행하기

실제 폰에서 앱을 테스트하는 가장 쉬운 방법입니다!

#### Android에서 실행하기

1. **Expo Go 앱 설치**
   - Google Play 스토어에서 "Expo Go" 검색
   - 무료 앱입니다. 다운로드하고 설치하세요

2. **같은 Wi-Fi 연결 확인**
   - 컴퓨터와 스마트폰이 **같은 Wi-Fi 네트워크**에 연결되어 있어야 합니다
   - 이것이 매우 중요합니다!

3. **QR 코드 스캔**
   - Expo Go 앱을 실행합니다
   - "Scan QR Code" 버튼을 탭합니다
   - 터미널에 표시된 QR 코드를 스캔합니다

4. **앱 실행 확인**
   - 앱이 자동으로 다운로드되고 실행됩니다
   - 첫 실행은 조금 느릴 수 있습니다 (정상입니다)

#### iOS (아이폰)에서 실행하기

1. **Expo Go 앱 설치**
   - App Store에서 "Expo Go" 검색
   - 무료 앱입니다. 다운로드하고 설치하세요

2. **같은 Wi-Fi 연결 확인**
   - 컴퓨터와 아이폰이 **같은 Wi-Fi 네트워크**에 연결되어 있어야 합니다

3. **카메라 앱으로 QR 코드 스캔**
   - 아이폰의 기본 카메라 앱을 실행합니다
   - 터미널의 QR 코드를 비춥니다
   - 화면 상단에 알림이 나타나면 탭합니다
   - Expo Go 앱이 자동으로 열리며 앱이 실행됩니다

### 6단계: 첫 번째 수정해보기

앱이 실행되면, 코드를 수정해서 변화를 확인해봅시다!

1. **코드 에디터 열기**
   - VS Code(Visual Studio Code)를 추천합니다
   - [VS Code 다운로드](https://code.visualstudio.com/)

2. **프로젝트 폴더 열기**
   - VS Code에서 `File` > `Open Folder` 선택
   - `my-first-app` 폴더를 선택합니다

3. **App.js 파일 열기**
   - 왼쪽 파일 목록에서 `App.js` 파일을 찾아 클릭합니다

4. **텍스트 수정하기**

원래 코드:
```jsx
import { StatusBar } from 'expo-status-bar';
import { StyleSheet, Text, View } from 'react-native';

export default function App() {
  return (
    <View style={styles.container}>
      <Text>Open up App.js to start working on your app!</Text>
      <StatusBar style="auto" />
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#fff',
    alignItems: 'center',
    justifyContent: 'center',
  },
});
```

이렇게 수정해보세요:
```jsx
import { StatusBar } from 'expo-status-bar';
import { StyleSheet, Text, View } from 'react-native';

export default function App() {
  return (
    <View style={styles.container}>
      <Text style={styles.title}>안녕하세요! React Native입니다! 🎉</Text>
      <Text style={styles.subtitle}>내 첫 번째 앱이에요!</Text>
      <StatusBar style="auto" />
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#4A90E2',  // 파란색 배경으로 변경
    alignItems: 'center',
    justifyContent: 'center',
  },
  title: {
    fontSize: 24,                // 큰 글씨
    fontWeight: 'bold',          // 굵은 글씨
    color: 'white',              // 흰색 글자
    marginBottom: 10,            // 아래쪽 여백
  },
  subtitle: {
    fontSize: 16,                // 중간 크기 글씨
    color: 'white',              // 흰색 글자
  },
});
```

**코드 설명**:

- `import { ... } from 'react-native'`: React Native에서 제공하는 컴포넌트들을 가져옵니다
  - `StyleSheet`: 스타일을 효율적으로 관리하는 도구
  - `Text`: 텍스트를 표시하는 컴포넌트
  - `View`: 컨테이너 역할을 하는 컴포넌트 (HTML의 `<div>`와 비슷)

- `export default function App()`: App이라는 함수형 컴포넌트를 정의하고 내보냅니다
  - `export default`: 다른 파일에서 이 컴포넌트를 사용할 수 있게 합니다
  - `function App()`: App이라는 이름의 함수를 만듭니다

- `<View style={styles.container}>`: container 스타일이 적용된 View 컴포넌트입니다
  - `{}`: 중괄호 안에는 JavaScript 코드를 작성합니다
  - `styles.container`: 아래에 정의된 container 스타일을 참조합니다

- `<Text style={styles.title}>`: title 스타일이 적용된 Text 컴포넌트입니다

- `StyleSheet.create({ ... })`: 스타일 객체를 생성합니다
  - React Native가 스타일을 최적화할 수 있게 해줍니다

- `flex: 1`: 사용 가능한 전체 공간을 차지합니다
- `backgroundColor`: 배경색을 설정합니다 (16진수 색상 코드)
- `alignItems: 'center'`: 가로 방향 가운데 정렬
- `justifyContent: 'center'`: 세로 방향 가운데 정렬

5. **저장하고 확인하기**
   - `Ctrl + S` (Windows) 또는 `Cmd + S` (Mac)로 저장합니다
   - 스마트폰을 확인하면 앱이 자동으로 업데이트됩니다!
   - 이것이 **Hot Reloading**입니다 - 매우 편리하죠!

---

## 방법 2: React Native CLI로 설치하기 (고급 사용자용)

더 많은 제어권이 필요하거나 특수한 네이티브 기능을 사용해야 한다면 React Native CLI를 사용할 수 있습니다.

### 주의사항

React Native CLI는 설정이 복잡합니다. 처음 배우는 분이라면 Expo를 먼저 사용해보시고, 나중에 필요할 때 CLI로 전환하는 것을 권장합니다.

### 사전 준비사항

#### 공통 (Windows와 Mac 모두)

1. **Node.js 설치** (위의 Expo 가이드와 동일)
2. **Watchman 설치** (Mac만 해당)

Watchman은 파일 변경을 감지하는 도구입니다.

```bash
brew install watchman
```

**코드 설명**:
- `brew install`: Homebrew로 프로그램을 설치하는 명령어입니다
- `watchman`: 파일 시스템의 변경사항을 모니터링하는 도구입니다

#### Windows 전용 설정

**1. JDK (Java Development Kit) 설치**

Android 앱을 빌드하려면 Java가 필요합니다.

1. [Azul Zulu OpenJDK 17](https://www.azul.com/downloads/?package=jdk#zulu) 다운로드
   - Windows용 MSI 설치 파일을 선택하세요
2. 설치 후 환경 변수 설정:

```bash
# 시스템 환경 변수에 추가
JAVA_HOME=C:\Program Files\Zulu\zulu-17
```

**환경 변수 설정 방법**:
1. `Win + R`을 누르고 `sysdm.cpl` 입력
2. "고급" 탭 선택
3. "환경 변수" 버튼 클릭
4. "시스템 변수"에서 "새로 만들기" 클릭
5. 변수 이름: `JAVA_HOME`
6. 변수 값: JDK 설치 경로

**환경 변수란?**
컴퓨터가 프로그램을 찾을 수 있도록 경로를 알려주는 설정입니다. JAVA_HOME은 Java가 어디에 설치되어 있는지 알려줍니다.

**2. Android Studio 설치**

1. [Android Studio](https://developer.android.com/studio) 다운로드
2. 설치 시 다음 항목들을 체크:
   - Android SDK
   - Android SDK Platform
   - Android Virtual Device

3. Android Studio 실행 후 SDK Manager 열기:
   - `Tools` > `SDK Manager` 또는 환영 화면에서 `More Actions` > `SDK Manager`

4. SDK Platforms 탭에서 설치:
   - `Android 13.0 (Tiramisu)` 체크
   - `Show Package Details` 체크
   - 다음 항목들 선택:
     - `Android SDK Platform 33`
     - `Intel x86 Atom_64 System Image` 또는 `Google APIs Intel x86 Atom System Image`

5. SDK Tools 탭에서 설치:
   - `Android SDK Build-Tools`
   - `Android Emulator`
   - `Android SDK Platform-Tools`

6. 환경 변수 설정:

```bash
# 시스템 환경 변수에 추가
ANDROID_HOME=C:\Users\사용자이름\AppData\Local\Android\Sdk

# Path 변수에 추가
%ANDROID_HOME%\platform-tools
%ANDROID_HOME%\emulator
%ANDROID_HOME%\tools
%ANDROID_HOME%\tools\bin
```

**Path 변수 추가 방법**:
1. 환경 변수 창에서 "Path" 변수를 찾아 선택
2. "편집" 버튼 클릭
3. "새로 만들기" 버튼 클릭
4. 위의 경로들을 하나씩 추가

#### Mac 전용 설정

**1. Xcode 설치 (iOS 개발용)**

1. Mac App Store에서 Xcode 검색
2. 설치 (용량이 크므로 시간이 오래 걸립니다)
3. Xcode를 한 번 실행하여 추가 구성 요소 설치
4. Command Line Tools 설치:

```bash
xcode-select --install
```

**코드 설명**:
- `xcode-select`: Xcode 개발자 도구를 관리하는 명령어입니다
- `--install`: Command Line Tools를 설치하는 옵션입니다

**2. CocoaPods 설치 (iOS 의존성 관리)**

```bash
sudo gem install cocoapods
```

**코드 설명**:
- `sudo`: 관리자 권한으로 실행 (비밀번호 입력 필요)
- `gem`: Ruby 패키지 관리자 (Mac에 기본 포함)
- `install cocoapods`: CocoaPods를 설치합니다

CocoaPods는 iOS 프로젝트의 라이브러리를 관리하는 도구입니다.

**3. Android Studio 설치 (Android 개발용)**

Windows와 동일하게 진행하되, 환경 변수는 다음과 같이 설정합니다:

```bash
# ~/.zshrc 또는 ~/.bash_profile 파일에 추가
export ANDROID_HOME=$HOME/Library/Android/sdk
export PATH=$PATH:$ANDROID_HOME/emulator
export PATH=$PATH:$ANDROID_HOME/platform-tools
export PATH=$PATH:$ANDROID_HOME/tools
export PATH=$PATH:$ANDROID_HOME/tools/bin
```

**파일 수정 방법**:
1. 터미널에서 다음 명령어 실행:
```bash
nano ~/.zshrc
```

2. 위의 내용을 파일 끝에 추가합니다
3. `Ctrl + X`를 누르고 `Y`를 입력한 후 엔터로 저장합니다
4. 터미널을 닫고 다시 열어 적용합니다

또는:
```bash
source ~/.zshrc
```

**코드 설명**:
- `export`: 환경 변수를 설정하는 명령어입니다
- `$HOME`: 사용자 홈 디렉토리를 의미합니다
- `$PATH`: 실행 파일을 찾는 경로 목록입니다
- `:`: 경로 구분자입니다
- `source`: 설정 파일을 즉시 적용하는 명령어입니다

### React Native 프로젝트 생성하기

이제 모든 준비가 끝났습니다! 프로젝트를 만들어봅시다.

```bash
npx @react-native-community/cli@latest init MyApp
```

**코드 설명**:
- `@react-native-community/cli`: React Native 커뮤니티에서 제공하는 공식 CLI 도구입니다
- `@latest`: 항상 최신 버전을 사용합니다
- `init`: 새 프로젝트를 초기화합니다
- `MyApp`: 프로젝트 이름 (원하는 이름으로 변경 가능)

**프로젝트 생성 중**:
- 필요한 템플릿과 의존성이 다운로드됩니다
- 몇 분 정도 걸릴 수 있습니다
- iOS 프로젝트의 경우 CocoaPods 설치도 진행됩니다

### 프로젝트 실행하기

#### Android에서 실행

1. Android 에뮬레이터를 먼저 실행합니다:
   - Android Studio > `Device Manager`
   - 가상 기기 생성 (없다면)
   - 실행 버튼 클릭

2. 프로젝트 폴더로 이동:
```bash
cd MyApp
```

3. Metro 번들러 시작:
```bash
npm start
```

**Metro란?**
Metro는 JavaScript 코드를 묶고(bundle) 최적화하는 도구입니다. React Native 앱의 모든 JavaScript 파일을 하나로 합쳐서 앱에서 실행할 수 있게 만듭니다.

4. 새 터미널 창을 열고 Android 앱 실행:
```bash
npm run android
```

**또는**:
```bash
npx react-native run-android
```

**명령어 설명**:
- `npm run android`: package.json에 정의된 android 스크립트를 실행합니다
- 앱을 빌드하고 에뮬레이터에 설치합니다
- 처음 빌드는 시간이 오래 걸립니다 (5~10분)

#### iOS에서 실행 (Mac만 가능)

1. 프로젝트 폴더로 이동:
```bash
cd MyApp
```

2. iOS 의존성 설치:
```bash
cd ios
bundle install
bundle exec pod install
cd ..
```

**코드 한 줄씩 설명**:
- `cd ios`: iOS 프로젝트 폴더로 이동합니다
- `bundle install`: Ruby 의존성을 설치합니다
- `bundle exec pod install`: CocoaPods 의존성을 설치합니다
  - 이 과정에서 필요한 iOS 라이브러리들이 다운로드됩니다
- `cd ..`: 상위 폴더(프로젝트 루트)로 돌아갑니다

3. Metro 번들러 시작:
```bash
npm start
```

4. 새 터미널 창을 열고 iOS 앱 실행:
```bash
npm run ios
```

**또는 특정 시뮬레이터 지정**:
```bash
npx react-native run-ios --simulator="iPhone 15"
```

**명령어 설명**:
- `--simulator`: 특정 시뮬레이터를 지정하는 옵션입니다
- 사용 가능한 시뮬레이터 목록 확인:
```bash
xcrun simctl list devices
```

### 개발 서버가 실행된 상태에서

앱이 실행되면 다음과 같은 기능들을 사용할 수 있습니다:

**개발자 메뉴 열기**:
- Android 에뮬레이터: `Ctrl + M` (Windows) 또는 `Cmd + M` (Mac)
- iOS 시뮬레이터: `Cmd + D`
- 실제 기기에서: 흔들기 (Shake)

**개발자 메뉴 옵션**:
- `Reload`: 앱 다시 로드
- `Debug`: Chrome DevTools로 디버깅
- `Show Inspector`: 요소 검사 도구
- `Enable Hot Reloading`: 자동 새로고침 활성화

---

## 프로젝트 구조 이해하기

React Native 프로젝트를 만들면 다음과 같은 구조가 생성됩니다.

### Expo 프로젝트 구조

```
my-first-app/
├── node_modules/          # 설치된 패키지들 (직접 수정하지 않습니다)
├── .expo/                 # Expo 관련 설정 (자동 생성)
├── assets/                # 이미지, 폰트 등 리소스 파일
│   ├── icon.png          # 앱 아이콘
│   ├── splash.png        # 스플래시 화면 이미지
│   └── adaptive-icon.png # Android 적응형 아이콘
├── App.js                 # 앱의 메인 파일 (시작점)
├── app.json               # 앱 설정 파일
├── package.json           # 프로젝트 정보와 의존성 목록
├── package-lock.json      # 의존성의 정확한 버전 정보
└── .gitignore             # Git에서 제외할 파일 목록
```

**주요 파일 설명**:

**App.js**:
```jsx
// 앱의 시작점이 되는 파일입니다
// 여기서 UI를 정의하고 로직을 작성합니다
import { StatusBar } from 'expo-status-bar';
import { StyleSheet, Text, View } from 'react-native';

export default function App() {
  // 이 함수가 반환하는 내용이 화면에 표시됩니다
  return (
    <View style={styles.container}>
      <Text>Open up App.js to start working on your app!</Text>
      <StatusBar style="auto" />
    </View>
  );
}
```

**app.json**:
```json
{
  "expo": {
    "name": "my-first-app",        // 앱 이름
    "slug": "my-first-app",         // 고유 식별자
    "version": "1.0.0",             // 앱 버전
    "orientation": "portrait",      // 화면 방향 (portrait: 세로, landscape: 가로)
    "icon": "./assets/icon.png",    // 앱 아이콘 경로
    "splash": {                     // 스플래시 화면 설정
      "image": "./assets/splash.png",
      "resizeMode": "contain",
      "backgroundColor": "#ffffff"
    },
    "platforms": ["ios", "android", "web"]  // 지원 플랫폼
  }
}
```

**package.json**:
```json
{
  "name": "my-first-app",
  "version": "1.0.0",
  "main": "node_modules/expo/AppEntry.js",  // 앱 진입점
  "scripts": {
    "start": "expo start",      // 개발 서버 시작
    "android": "expo start --android",  // Android에서 실행
    "ios": "expo start --ios",  // iOS에서 실행
    "web": "expo start --web"   // 웹에서 실행
  },
  "dependencies": {             // 프로젝트에서 사용하는 라이브러리들
    "expo": "~51.0.0",
    "react": "18.2.0",
    "react-native": "0.74.0"
  }
}
```

### React Native CLI 프로젝트 구조

```
MyApp/
├── android/               # Android 네이티브 코드
├── ios/                   # iOS 네이티브 코드
├── node_modules/          # 설치된 패키지들
├── App.tsx (또는 App.js) # 앱의 메인 파일
├── index.js               # 앱의 진입점
├── package.json           # 프로젝트 정보
├── metro.config.js        # Metro 번들러 설정
└── babel.config.js        # Babel 설정 (JavaScript 변환 도구)
```

**추가 파일 설명**:

**index.js**:
```jsx
// 앱의 실제 진입점입니다
// App 컴포넌트를 등록하는 역할을 합니다
import {AppRegistry} from 'react-native';
import App from './App';
import {name as appName} from './app.json';

// App 컴포넌트를 네이티브 모듈에 등록합니다
AppRegistry.registerComponent(appName, () => App);
```

**metro.config.js**:
```javascript
// Metro 번들러의 설정 파일입니다
// JavaScript 코드를 어떻게 번들링할지 정의합니다
const {getDefaultConfig, mergeConfig} = require('@react-native/metro-config');

const config = {};

module.exports = mergeConfig(getDefaultConfig(__dirname), config);
```

**babel.config.js**:
```javascript
// Babel 설정 파일입니다
// 최신 JavaScript 문법을 구버전으로 변환합니다
module.exports = {
  presets: ['module:@react-native/babel-preset'],
};
```

---

## 문제 해결 (Troubleshooting)

개발 중 자주 발생하는 문제들과 해결 방법입니다.

### 일반적인 문제

#### 1. "command not found" 에러

**증상**:
```bash
npx: command not found
```

**원인**: Node.js가 제대로 설치되지 않았거나 PATH에 추가되지 않았습니다.

**해결방법**:
1. Node.js를 다시 설치합니다
2. 설치 시 "Add to PATH" 옵션을 반드시 체크합니다
3. 터미널을 완전히 닫고 다시 엽니다

#### 2. 네트워크 연결 문제 (Expo)

**증상**: 폰에서 QR 코드를 스캔했는데 연결이 안 됩니다.

**해결방법**:
1. 컴퓨터와 스마트폰이 **같은 Wi-Fi**에 연결되어 있는지 확인
2. 방화벽이 연결을 차단하고 있는지 확인
3. `npx expo start --tunnel` 명령어로 터널 모드 사용:

```bash
npx expo start --tunnel
```

**코드 설명**:
- `--tunnel`: Expo 서버를 인터넷을 통해 접근할 수 있게 합니다
- 같은 Wi-Fi가 아니어도 연결 가능합니다
- 속도는 조금 느릴 수 있습니다

#### 3. 포트가 이미 사용 중

**증상**:
```
Port 8081 is already in use
```

**원인**: 다른 React Native 프로젝트가 이미 실행 중입니다.

**해결방법**:
1. 기존 Metro 서버를 종료합니다 (`Ctrl + C`)
2. 포트를 사용 중인 프로세스를 찾아 종료합니다:

**Windows**:
```bash
netstat -ano | findstr :8081
taskkill /PID [프로세스ID] /F
```

**Mac**:
```bash
lsof -i :8081
kill -9 [프로세스ID]
```

**코드 설명**:
- `netstat`: 네트워크 연결 상태를 확인하는 명령어
- `lsof`: 열려있는 파일과 프로세스를 확인하는 명령어
- `kill -9`: 프로세스를 강제 종료합니다
- `taskkill /F`: Windows에서 프로세스를 강제 종료합니다

### Android 관련 문제

#### 1. Gradle 빌드 실패

**증상**:
```
FAILURE: Build failed with an exception
```

**해결방법**:

1. **Gradle 캐시 삭제**:
```bash
cd android
./gradlew clean
cd ..
```

**코드 설명**:
- `./gradlew`: Gradle Wrapper를 실행합니다
- `clean`: 이전 빌드 결과물을 삭제합니다

2. **Android Studio에서 프로젝트 다시 동기화**:
   - Android Studio에서 `android` 폴더 열기
   - `File` > `Sync Project with Gradle Files`

3. **의존성 다시 설치**:
```bash
rm -rf node_modules
npm install
```

#### 2. SDK 버전 문제

**증상**:
```
SDK location not found
```

**해결방법**:

`android/local.properties` 파일에 SDK 경로를 추가합니다:

```properties
sdk.dir=C:\\Users\\사용자이름\\AppData\\Local\\Android\\Sdk
```

**주의**: Windows에서는 백슬래시(`\`)를 두 번 사용합니다 (`\\`)

### iOS 관련 문제 (Mac)

#### 1. CocoaPods 설치 실패

**증상**:
```
[!] Unable to find a specification for...
```

**해결방법**:

1. **CocoaPods 리포지토리 업데이트**:
```bash
pod repo update
```

2. **캐시 삭제 후 재설치**:
```bash
cd ios
rm -rf Pods
rm Podfile.lock
pod install
cd ..
```

**코드 설명**:
- `pod repo update`: CocoaPods 라이브러리 목록을 최신으로 업데이트합니다
- `rm -rf Pods`: Pods 폴더를 삭제합니다
- `rm Podfile.lock`: 잠금 파일을 삭제합니다
- `pod install`: 의존성을 다시 설치합니다

#### 2. Xcode 빌드 오류

**증상**:
```
error: Build input file cannot be found
```

**해결방법**:

1. **빌드 폴더 삭제**:
```bash
cd ios
rm -rf build
cd ..
```

2. **Xcode에서 Clean Build**:
   - Xcode 실행
   - `Product` > `Clean Build Folder` (Shift + Cmd + K)
   - 다시 빌드 시도

3. **Derived Data 삭제**:
```bash
rm -rf ~/Library/Developer/Xcode/DerivedData
```

#### 3. Rosetta 2 관련 (M1/M2 Mac)

**증상**: Apple Silicon(M1/M2) Mac에서 오류 발생

**해결방법**:

Rosetta 2 설치:
```bash
softwareupdate --install-rosetta
```

**코드 설명**:
- Apple Silicon Mac에서 Intel 기반 앱을 실행할 수 있게 해주는 도구입니다

---

## 유용한 개발 도구

### 코드 에디터 확장 프로그램

**VS Code 추천 확장**:

1. **ES7+ React/Redux/React-Native snippets**
   - 코드 자동 완성
   - 빠른 컴포넌트 생성

2. **React Native Tools**
   - 디버깅 지원
   - 코드 실행

3. **Prettier**
   - 코드 자동 포맷팅
   - 일관된 코드 스타일 유지

4. **ESLint**
   - 코드 오류 검사
   - 베스트 프랙티스 제안

### React Native 디버깅 도구

**Reactotron** (추천):
```bash
npm install --save-dev reactotron-react-native
```

앱 상태, API 호출, 비동기 작업 등을 모니터링할 수 있는 강력한 도구입니다.

**Flipper**:
React Native 팀에서 공식적으로 제공하는 디버깅 플랫폼입니다.
- 네트워크 요청 모니터링
- Layout 검사
- 로그 확인

---

## 다음 단계

축하합니다! React Native 개발 환경 설정을 완료했습니다! 🎉

**이제 무엇을 배워야 할까요?**

1. **기본 컴포넌트 익히기**
   - View, Text, Image, ScrollView 등
   - 다음 문서에서 자세히 다룹니다

2. **레이아웃 시스템 (Flexbox)**
   - 화면을 어떻게 배치하는지 배웁니다

3. **사용자 입력 처리하기**
   - Button, TextInput, TouchableOpacity

4. **상태 관리 (State)**
   - useState, useEffect 등 React Hooks

5. **내비게이션**
   - 화면 간 이동하는 방법

6. **스타일링**
   - 아름다운 UI 만들기

**추천 학습 자료**:
- [React Native 공식 문서](https://reactnative.dev/)
- [Expo 공식 문서](https://docs.expo.dev/)
- [React 공식 문서](https://react.dev/)

이제 본격적으로 앱 개발을 시작할 준비가 되었습니다! 다음 문서에서 React Native의 핵심 개념들을 배워보겠습니다.