---
title: "반환값 유무에 따른 함수"
description: "반환값이 있는 함수와 없는 함수의 차이를 명확히 구분해보세요"
slug: "c-function-with-without-return"
sidebar_position: 1
date: "2024-01-16"
---

# 반환값 유무에 따른 함수

## 🎯 함수의 두 가지 유형

함수는 **반환값이 있는지 없는지**에 따라 크게 두 가지로 분류됩니다.

## 1️⃣ 반환값이 없는 함수 (void)

### 개념

반환값이 없는 함수는 **반환형이 void**입니다.

```c
void function_name() {
    // 함수의 기능만 수행
    // 값을 돌려주지 않음
}
```

### 특징

- **반환형:** void
- **역할:** 작업만 수행하고 결과를 돌려주지 않음
- **사용 예:** 화면 출력, 파일 저장 등

### 예제 1: 메시지 출력 함수

```c
#include <stdio.h>

void print_welcome() {
    printf("안녕하세요!\n");
    printf("함수 실습입니다.\n");
}

int main() {
    print_welcome();
    print_welcome();
    
    return 0;
}
```

**코드 분석:**
```c
void print_welcome() {
    // void: 반환값이 없음
    printf("안녕하세요!\n");
}
```

**실행 결과:**
```
안녕하세요!
함수 실습입니다.
안녕하세요!
함수 실습입니다.
```

### 예제 2: 학생 정보 출력 함수

```c
#include <stdio.h>

void print_student_info(char name[], int score) {
    printf("이름: %s\n", name);
    printf("점수: %d점\n", score);
    printf("\n");
}

int main() {
    print_student_info("김철수", 85);
    print_student_info("이영희", 92);
    
    return 0;
}
```

**코드 분석:**
```c
void print_student_info(char name[], int score) {
    // void: 반환값이 없으므로 return 문을 쓸 수 없음
    printf("이름: %s\n", name);
    printf("점수: %d점\n", score);
}
```

**실행 결과:**
```
이름: 김철수
점수: 85점

이름: 이영희
점수: 92점

```

## 2️⃣ 반환값이 있는 함수

### 개념

반환값이 있는 함수는 **반환형이 int, float, double 등의 자료형**입니다.

```c
int function_name() {
    return 결과값;
}
```

### 특징

- **반환형:** int, float, double, char 등
- **역할:** 작업을 수행하고 **결과값을 돌려줌**
- **사용 예:** 계산 결과 반환, 검사 결과 반환 등

### 예제 1: 덧셈 함수

```c
#include <stdio.h>

int add(int a, int b) {
    int sum = a + b;
    return sum;
}

int main() {
    int result = add(10, 20);
    printf("10 + 20 = %d\n", result);
    
    return 0;
}
```

**코드 분석:**
```c
int add(int a, int b) {
    // int: 정수형 값을 반환하겠다는 뜻
    int sum = a + b;
    return sum;
    // return: 계산 결과를 돌려줌
}
```

```c
int result = add(10, 20);
// 반환된 30이 result에 저장됨
```

**실행 결과:**
```
10 + 20 = 30
```

### 예제 2: 직접 반환

```c
#include <stdio.h>

int multiply(int x, int y) {
    return x * y;
}

int main() {
    int product = multiply(5, 6);
    printf("5 × 6 = %d\n", product);
    
    return 0;
}
```

**코드 분석:**
```c
return x * y;
// 계산을 수행하면서 바로 반환
// 변수에 저장하는 단계를 생략할 수 있음
```

**실행 결과:**
```
5 × 6 = 30
```

### 예제 3: 실수형 반환

```c
#include <stdio.h>

double divide(double a, double b) {
    return a / b;
}

int main() {
    double result = divide(10.0, 3.0);
    printf("10 ÷ 3 = %.2f\n", result);
    
    return 0;
}
```

**코드 분석:**
```c
double divide(double a, double b) {
    // double: 실수형 값을 반환
    return a / b;
}
```

**실행 결과:**
```
10 ÷ 3 = 3.33
```

## ⚖️ 비교: void vs 반환값 있는 함수

### 호출 방식의 차이

#### void 함수

```c
void greet(char name[]) {
    printf("안녕, %s!\n", name);
}

int main() {
    greet("철수");
    // 반환값이 없으므로 그냥 호출만 함
    
    return 0;
}
```

#### 반환값이 있는 함수

```c
int get_age(int birth_year) {
    return 2024 - birth_year;
}

int main() {
    int age = get_age(2000);
    // 반환값을 변수에 저장
    
    return 0;
}
```

### 차이점 정리

| 특징 | void 함수 | 반환값 있는 함수 |
|------|---------|----------------|
| **반환형** | void | int, float, double 등 |
| **반환값** | 없음 | 있음 |
| **return 사용** | return; (값 없음) | return 값; |
| **호출 방식** | `함수명();` | `변수 = 함수명();` |
| **사용 목적** | 작업 수행만 필요 | 결과값이 필요함 |

## 🔄 반환값을 받는 여러 방법

### 방법 1: 변수에 저장 후 사용

```c
int result = add(5, 3);
printf("결과: %d\n", result);
```

### 방법 2: 직접 출력

```c
printf("결과: %d\n", add(5, 3));
// 반환값을 직접 printf에 전달
```

### 방법 3: 연산에 사용

```c
int total = add(5, 3) + add(10, 20);
// 함수 호출 결과를 더함
```

## ⚠️ 흔한 실수

### 실수 1: void 함수에서 반환값 받으려고 함

```c
void print_number(int num) {
    printf("%d\n", num);
}

int main() {
    int result = print_number(5);  // ❌ 에러!
    
    print_number(5);  // ✅ 올바른 사용
    return 0;
}
```

### 실수 2: 반환값을 무시함

```c
int add(int a, int b) {
    return a + b;
}

int main() {
    add(5, 3);  // ❌ 나쁜 코드 (반환값을 버림)
    
    int result = add(5, 3);  // ✅ 좋은 코드
    return 0;
}
```

### 실수 3: 반환형과 맞지 않는 값 반환

```c
int get_number() {
    return 3.14;  // 정수형으로 변환되어 3이 반환됨
}

float get_value() {
    return 10;  // 10.0이 반환됨
}
```

## 📝 언제 어떤 함수를 사용할까?

### void 함수를 사용하는 경우

```c
void print_menu() {
    printf("1. 더하기\n");
    printf("2. 빼기\n");
    printf("3. 곱하기\n");
}

void save_to_file(char filename[]) {
    FILE *fp = fopen(filename, "w");
    fclose(fp);
}
```

**특징:** 결과값이 필요 없고 작업 수행만 필요

### 반환값이 있는 함수를 사용하는 경우

```c
int calculate_total(int price, int quantity) {
    return price * quantity;
}

int is_prime(int num) {
    return result;
}
```

**특징:** 계산 결과나 검사 결과가 필요함

## 📌 핵심 정리

:::note 기억하세요!
1. **void 함수** = 값을 돌려주지 않는 함수
2. **반환값 있는 함수** = 값을 돌려주는 함수
3. **반환형과 return의 일치** = 매우 중요
4. **호출 방식이 다름** = void와 반환값 함수는 사용 방법이 다름
5. **필요에 따라 선택** = 목적에 맞는 함수를 선택하세요
:::

---

**작성일:** 2024.01.16  
**난이도:** ⭐⭐ 초급