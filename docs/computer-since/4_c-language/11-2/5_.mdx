---
title: "함수 실습 예제"
description: "반환값, 매개변수, 주소 전달 등을 활용한 다양한 실습 예제"
slug: "c-function-practice-advanced"
sidebar_position: 5
date: "2024-01-16"
---

# 함수 실습 예제

## 📝 예제 1: 구구단 프로그램

### 문제

사용자로부터 단(1~9)을 입력받아 그 단의 구구단을 출력하는 프로그램을 만들어보세요.

### 풀이

```c
#include <stdio.h>

// 함수 원형
void print_multiplication_table(int dan);

// main 함수
int main() {
    int dan;
    
    printf("단을 입력하세요 (1~9): ");
    scanf("%d", &dan);  // 단 입력
    
    print_multiplication_table(dan);  // 함수 호출
    
    return 0;
}

// 함수 정의
void print_multiplication_table(int dan) {
    // dan: 입력받은 단 (값으로 전달됨)
    
    printf("\n=== %d단 ===\n", dan);
    
    for (int i = 1; i <= 9; i++) {
        printf("%d × %d = %d\n", dan, i, dan * i);
        // ↑ dan * i: 단과 수를 곱함
    }
}
```

### 코드 분석

```c
void print_multiplication_table(int dan) {
    // void: 반환값 없음 (출력만 함)
    // int dan: 단을 나타내는 정수
    
    for (int i = 1; i <= 9; i++) {
        // i가 1부터 9까지 반복
        printf("%d × %d = %d\n", dan, i, dan * i);
    }
}
```

```c
printf("단을 입력하세요 (1~9): ");
scanf("%d", &dan);
// ↑ &dan: dan의 주소 (scanf는 주소가 필요함)

print_multiplication_table(dan);
// ↑ dan의 값으로 전달
```

### 실행 결과

```
단을 입력하세요 (1~9): 3

=== 3단 ===
3 × 1 = 3
3 × 2 = 6
3 × 3 = 9
3 × 4 = 12
3 × 5 = 15
3 × 6 = 18
3 × 7 = 21
3 × 8 = 24
3 × 9 = 27
```

---

## 📝 예제 2: 팩토리얼 계산

### 문제

팩토리얼을 계산하는 함수를 만들고, 사용자가 입력한 수의 팩토리얼을 구해보세요.

### 풀이

```c
#include <stdio.h>

// 함수 원형
long factorial(int n);  // long: 큰 수를 처리하기 위함

// main 함수
int main() {
    int num;
    
    printf("팩토리얼을 계산할 수를 입력하세요: ");
    scanf("%d", &num);
    
    // 방법 1: 결과를 변수에 저장
    long result = factorial(num);
    printf("%d! = %ld\n", num, result);
    
    // 방법 2: 직접 출력
    printf("%d! = %ld\n", num, factorial(num));
    
    return 0;
}

// 함수 정의
long factorial(int n) {
    // n: 팩토리얼을 구할 수 (값으로 전달)
    // long: 반환형 (큰 수 처리 가능)
    
    long result = 1;
    
    for (int i = 1; i <= n; i++) {
        result = result * i;
        // ↑ 1부터 n까지 모두 곱함
    }
    
    return result;  // 계산된 팩토리얼 반환
}
```

### 코드 분석

```c
long factorial(int n) {
    // long: 반환형 (int보다 큰 값 처리 가능)
    //       3! = 6, 5! = 120, 10! = 3628800...
    
    long result = 1;
    
    for (int i = 1; i <= n; i++) {
        result = result * i;
        // 1 × 2 × 3 × ... × n 계산
    }
    
    return result;  // 반환값 있음
}
```

```c
long result = factorial(num);
// ↑ 반환된 팩토리얼 값을 result에 저장

printf("%d! = %ld\n", num, factorial(num));
// ↑ 함수 호출 결과를 직접 printf에 전달
// ↑ %ld: long 형식 (d가 아니라 ld)
```

### 실행 결과

```
팩토리얼을 계산할 수를 입력하세요: 5
5! = 120
5! = 120
```

---

## 📝 예제 3: 값에 의한 전달 vs 주소에 의한 전달

### 코드

```c
#include <stdio.h>

// 함수 원형
void call_by_value(int num);        // 값으로 전달
void call_by_address(int *num);     // 주소로 전달

// main 함수
int main() {
    int a = 10;
    
    printf("=== 값에 의한 전달 ===\n");
    printf("함수 호출 전: a = %d\n", a);
    call_by_value(a);       // a의 값을 전달
    printf("함수 호출 후: a = %d\n\n", a);  // a는 변화 없음
    
    printf("=== 주소에 의한 전달 ===\n");
    printf("함수 호출 전: a = %d\n", a);
    call_by_address(&a);    // a의 주소를 전달
    printf("함수 호출 후: a = %d\n", a);   // a가 변화됨
    
    return 0;
}

// 함수 1: 값으로 전달
void call_by_value(int num) {
    printf("  함수 내부: num = %d\n", num);
    num = num + 1;
    printf("  함수 수정 후: num = %d (main의 a는 변화 없음)\n", num);
}

// 함수 2: 주소로 전달
void call_by_address(int *num) {
    printf("  함수 내부: *num = %d\n", *num);
    *num = *num + 1;
    printf("  함수 수정 후: *num = %d (main의 a도 변화됨)\n", *num);
}
```

### 코드 분석

```c
// 값으로 전달
call_by_value(a);
// ↑ a의 값(10)만 복사해서 전달
// ↑ 함수 내부에서 수정해도 a는 변화 없음

// 주소로 전달
call_by_address(&a);
// ↑ a의 주소를 전달
// ↑ 함수 내부에서 *num을 수정하면 a도 변화됨
```

```c
void call_by_value(int num) {
    num = num + 1;  // 복사본을 수정
}

void call_by_address(int *num) {
    *num = *num + 1;  // 원본을 수정
}
```

### 실행 결과

```
=== 값에 의한 전달 ===
함수 호출 전: a = 10
  함수 내부: num = 10
  함수 수정 후: num = 11 (main의 a는 변화 없음)
함수 호출 후: a = 10

=== 주소에 의한 전달 ===
함수 호출 전: a = 10
  함수 내부: *num = 10
  함수 수정 후: *num = 11 (main의 a도 변화됨)
함수 호출 후: a = 11
```

---

## 📝 예제 4: 포인터를 사용한 두 수 교환

### 코드

```c
#include <stdio.h>

// 함수 원형
void swap(int *a, int *b);  // 포인터 매개변수

// main 함수
int main() {
    int x = 5;
    int y = 10;
    
    printf("교환 전: x = %d, y = %d\n", x, y);
    swap(&x, &y);  // x, y의 주소를 전달
    printf("교환 후: x = %d, y = %d\n", x, y);
    
    return 0;
}

// 함수 정의
void swap(int *a, int *b) {
    // a: x의 주소, b: y의 주소
    
    int temp = *a;    // temp = x의 값
    // ↑ *a: a가 가리키는 주소의 값
    
    *a = *b;          // x의 값 = y의 값
    *b = temp;        // y의 값 = 원래 x의 값
}
```

### 코드 분석

```c
void swap(int *a, int *b) {
    // *a: a가 가리키는 곳의 값 (x)
    // *b: b가 가리키는 곳의 값 (y)
    
    int temp = *a;    // x 값을 임시 저장
    *a = *b;          // x = y
    *b = temp;        // y = 원래 x
}
```

### 실행 결과

```
교환 전: x = 5, y = 10
교환 후: x = 10, y = 5
```

---

## 📝 예제 5: 세 수 중 최댓값 찾기

### 코드

```c
#include <stdio.h>

// 함수 원형
int find_max(int a, int b, int c);  // 값으로 전달

// main 함수
int main() {
    int num1 = 25;
    int num2 = 15;
    int num3 = 30;
    
    int max = find_max(num1, num2, num3);
    // ↑ 세 수의 값을 전달
    // ↑ 반환된 최댓값을 max에 저장
    
    printf("최댓값: %d\n", max);
    
    return 0;
}

// 함수 정의
int find_max(int a, int b, int c) {
    int max = a;  // 일단 a를 최댓값으로 설정
    
    if (b > max) {
        max = b;  // b가 더 크면 b로 갱신
    }
    
    if (c > max) {
        max = c;  // c가 더 크면 c로 갱신
    }
    
    return max;  // 최댓값 반환
}
```

### 코드 분석

```c
int find_max(int a, int b, int c) {
    // int: 반환형 (최댓값 반환)
    // a, b, c: 값으로 전달됨
    
    int max = a;
    if (b > max) max = b;
    if (c > max) max = c;
    
    return max;  // 반환값 있음
}
```

### 실행 결과

```
최댓값: 30
```

---

## 📝 예제 6: 주소를 이용한 두 수의 합과 차 계산

### 코드

```c
#include <stdio.h>

// 함수 원형
void calculate(int a, int b, int *sum, int *diff);
// ↑ a, b: 값으로 전달 (계산할 수)
// ↑ sum, diff: 주소로 전달 (결과 저장)

// main 함수
int main() {
    int num1 = 20;
    int num2 = 8;
    int sum = 0;
    int diff = 0;
    
    printf("계산 전: sum = %d, diff = %d\n", sum, diff);
    
    calculate(num1, num2, &sum, &diff);
    // ↑ num1, num2: 값으로 전달
    // ↑ &sum, &diff: 주소로 전달
    
    printf("계산 후: sum = %d, diff = %d\n", sum, diff);
    
    return 0;
}

// 함수 정의
void calculate(int a, int b, int *sum, int *diff) {
    // a = 20, b = 8 (값으로 받음)
    // *sum, *diff: 원본을 가리킴 (주소로 받음)
    
    *sum = a + b;   // sum에 합 저장
    *diff = a - b;  // diff에 차 저장
}
```

### 코드 분석

```c
void calculate(int a, int b, int *sum, int *diff) {
    // a, b: 값으로 받음
    // *sum, *diff: 포인터로 받음
    
    *sum = a + b;    // main의 sum에 합 저장
    *diff = a - b;   // main의 diff에 차 저장
}
```

```c
calculate(num1, num2, &sum, &diff);
// ↑ num1(20), num2(8): 값으로 전달
// ↑ &sum, &diff: 주소로 전달
```

### 실행 결과

```
계산 전: sum = 0, diff = 0
계산 후: sum = 28, diff = 12
```

---

## 📌 핵심 정리

:::note 예제로 배운 내용
1. **void 함수** = 반환값 없이 작업만 수행 (구구단, 교환)
2. **반환값 있는 함수** = 결과값 반환 (팩토리얼, 최댓값)
3. **값에 의한 전달** = 원본 보호, 복사본 전달
4. **주소에 의한 전달** = 원본 수정, 여러 값 반환
5. **조합 사용** = 계산은 값으로, 결과는 주소로
:::

---

**작성일:** 2024.01.16  
**난이도:** ⭐⭐⭐ 중급