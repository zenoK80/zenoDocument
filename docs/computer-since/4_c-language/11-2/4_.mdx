---
title: "주소에 의한 전달"
description: "포인터를 사용해서 함수에 주소를 전달하고 원본을 수정하는 방법을 배워보세요"
slug: "c-function-call-by-address"
sidebar_position: 4
date: "2024-01-16"
---

# 주소에 의한 전달 (Call by Reference/Address)

## 🎯 주소에 의한 전달이란?

**주소에 의한 전달(Call by Address)**은 함수를 호출할 때 **인수의 주소(주소값)를 전달**하는 방식입니다.

```
원본 변수의 주소 → 함수로 전달 → 함수 내부에서 원본 수정 가능
```

## 📍 포인터와 주소 연산자

### 주소 연산자 (&)

```c
int a = 10;
int *ptr = &a;  // &a: a의 주소를 가져옴
```

**& 기호 의미:**
- `&변수` = "변수의 메모리 주소"
- 예: `&a` = "변수 a가 저장된 메모리 주소"

### 포인터 (*)

```c
int *ptr = &a;  // ptr은 포인터 (주소를 저장하는 변수)
printf("%d\n", *ptr);  // *ptr: 주소가 가리키는 값
```

**\* 기호 의미 (포인터 사용 시):**
- `*ptr` = "ptr이 가리키는 주소에 저장된 값"
- 예: `*ptr = 10` = "ptr이 가리키는 곳의 값을 10으로 변경"

## 🔄 값에 의한 전달 vs 주소에 의한 전달

### 값에 의한 전달 (이전 방식)

```c
void increment(int num) {
    num = num + 1;  // 복사본만 변경됨
}

int main() {
    int a = 0;
    increment(a);   // a의 값(0)을 전달
    printf("a = %d\n", a);  // 여전히 0
}
```

### 주소에 의한 전달 (새로운 방식)

```c
void increment(int *num) {
    *num = *num + 1;  // 원본을 변경
}

int main() {
    int a = 0;
    increment(&a);   // a의 주소를 전달
    printf("a = %d\n", a);  // 1로 변경됨!
}
```

## 🔄 예제 1: 값 증가시키기

### 코드

```c
#include <stdio.h>

// 함수 원형
void increment(int *num);  // 포인터 매개변수

// main 함수
int main() {
    int a = 0;
    
    printf("함수 호출 전: a = %d\n", a);
    increment(&a);  // a의 주소를 전달 (& 연산자 사용)
    printf("함수 호출 후: a = %d\n", a);
    
    return 0;
}

// 함수 정의
void increment(int *num) {
    // num: a의 주소를 받음
    
    printf("함수 내부 (호출 직후): *num = %d\n", *num);
    // *num: num이 가리키는 주소의 값 (즉, a의 값)
    
    *num = *num + 1;  // a의 값을 증가
    
    printf("함수 내부 (수정 후): *num = %d\n", *num);
}
```

### 코드 분석 (한 줄씩)

```c
void increment(int *num) {
    // ↑ int *num: 정수를 가리키는 포인터
    //   a의 주소를 받음
}
```

```c
increment(&a);
// ↑ &a: a의 주소를 전달
// ↑ 값이 아니라 주소를 보내는 것!
```

```c
*num = *num + 1;
// ↑ *num: num이 가리키는 주소의 값
//       (즉, 원본 변수 a의 값)
// ↑ 이 값을 1 증가
// ↑ 원본 a가 변경됨!
```

### 메모리 상태 변화

```
[함수 호출 전]
메모리 주소 1000: a = 0

[함수 호출]
메모리 주소 1000: a = 0
메모리 주소 2000: num = 1000 (a의 주소를 저장)

[함수 내부에서 값 증가]
*num = 0 + 1 = 1 실행
메모리 주소 1000: a = 1 ← 변경됨!
메모리 주소 2000: num = 1000 (주소는 변화 없음)

[함수 종료]
메모리 주소 1000: a = 1 (변경된 값이 유지됨)
```

### 실행 결과

```
함수 호출 전: a = 0
함수 내부 (호출 직후): *num = 0
함수 내부 (수정 후): *num = 1
함수 호출 후: a = 1
```

**결론:** 함수 내부에서 *num을 변경했고, 원본 a도 1로 변경되었습니다!

## 🔄 예제 2: 두 수 교환하기

### 코드

```c
#include <stdio.h>

// 함수 원형
void swap(int *a, int *b);  // 포인터 매개변수

// main 함수
int main() {
    int i = 2;
    int j = 3;
    
    printf("교환 전: i = %d, j = %d\n", i, j);
    swap(&i, &j);  // i와 j의 주소를 전달
    printf("교환 후: i = %d, j = %d\n", i, j);
    
    return 0;
}

// 함수 정의
void swap(int *a, int *b) {
    // a: i의 주소, b: j의 주소
    
    printf("  함수 내부 교환 전: *a = %d, *b = %d\n", *a, *b);
    
    int temp = *a;    // temp = i의 값
    *a = *b;          // i의 값 = j의 값
    *b = temp;        // j의 값 = 원래 i의 값
    
    printf("  함수 내부 교환 후: *a = %d, *b = %d\n", *a, *b);
}
```

### 코드 분석 (swap 함수)

```c
void swap(int *a, int *b) {
    // a: i의 주소, b: j의 주소
    
    int temp = *a;    // *a: i의 값(2)
    *a = *b;          // i의 값 = j의 값(3)
    *b = temp;        // j의 값 = 원래 i의 값(2)
    // ↑ 모두 주소를 통해 원본을 변경!
}
```

```c
swap(&i, &j);
// ↑ &i: i의 주소
// ↑ &j: j의 주소
// ↑ 값이 아니라 주소를 전달
```

### 메모리 상태 변화

```
[교환 전]
메모리 1000: i = 2
메모리 1004: j = 3

[함수 호출]
메모리 1000: i = 2
메모리 1004: j = 3
메모리 2000: a = 1000 (i의 주소)
메모리 2004: b = 1004 (j의 주소)

[교환 수행]
*a = *b 실행
메모리 1000: i = 3 ← 변경됨!

*b = temp 실행
메모리 1004: j = 2 ← 변경됨!

[함수 종료]
메모리 1000: i = 3 (변경된 값 유지)
메모리 1004: j = 2 (변경된 값 유지)
```

### 실행 결과

```
교환 전: i = 2, j = 3
  함수 내부 교환 전: *a = 2, *b = 3
  함수 내부 교환 후: *a = 3, *b = 2
교환 후: i = 3, j = 2
```

**결론:** 이번에는 main의 i와 j가 실제로 교환되었습니다!

## 💡 주소에 의한 전달의 장점과 단점

### 장점

```c
✅ 원본 수정 가능
   함수에서 main의 변수를 직접 변경 가능

✅ 메모리 효율
   큰 배열도 주소만 전달하면 됨 (복사 필요 없음)

✅ 여러 값 반환
   포인터를 통해 여러 값을 반환할 수 있음
```

### 단점

```c
❌ 복잡성
   포인터 개념을 이해해야 함

❌ 오류 가능성
   잘못 사용하면 메모리 손상 가능

❌ 포인터 문법
   &와 * 연산자 사용이 필요
```

## 📊 주소 전달 문법 정리

### 함수 호출 시

```c
int a = 10;

// 값 전달
function(a);     // a의 값을 전달

// 주소 전달
function(&a);    // a의 주소를 전달
```

### 함수 정의 시

```c
// 값 매개변수
void function(int num) {
    // num: 받은 값
    printf("%d\n", num);
}

// 포인터 매개변수
void function(int *num) {
    // *num: 주소가 가리키는 값
    printf("%d\n", *num);
}
```

## 🎬 실제 예제: 두 값의 합 계산

### 코드

```c
#include <stdio.h>

// 함수 원형
void add_and_save(int a, int b, int *result);
// ↑ a, b: 값으로 받음
// ↑ result: 주소로 받음 (결과를 저장할 곳)

// main 함수
int main() {
    int num1 = 10;
    int num2 = 20;
    int sum = 0;
    
    printf("덧셈 전: sum = %d\n", sum);
    
    add_and_save(num1, num2, &sum);
    // ↑ sum의 주소를 전달해서 결과를 저장
    
    printf("덧셈 후: sum = %d\n", sum);
    
    return 0;
}

// 함수 정의
void add_and_save(int a, int b, int *result) {
    // a = 10, b = 20 (값으로 받음)
    // *result = sum의 원본을 가리킴
    
    int temp = a + b;  // 계산
    *result = temp;    // 원본 sum에 결과 저장
}
```

### 코드 분석

```c
void add_and_save(int a, int b, int *result) {
    // result는 포인터
    // *result: result가 가리키는 주소의 값 (즉, main의 sum)
    
    *result = a + b;
    // ↑ main의 sum에 직접 결과를 저장
}
```

```c
add_and_save(num1, num2, &sum);
// ↑ num1(10), num2(20): 값으로 전달
// ↑ &sum: sum의 주소로 전달
```

### 실행 결과

```
덧셈 전: sum = 0
덧셈 후: sum = 30
```

## 🔑 포인터 사용 규칙 정리

### 포인터 선언

```c
int *ptr;  // ptr은 int를 가리키는 포인터
```

### 주소 할당

```c
int a = 10;
int *ptr = &a;  // ptr에 a의 주소를 할당
```

### 값 접근

```c
printf("%d\n", *ptr);  // *ptr = 10 (a의 값)
```

### 값 변경

```c
*ptr = 20;  // a의 값을 20으로 변경
```

## 📌 핵심 정리

:::note 꼭 기억하세요!
1. **& (주소 연산자)** = 변수의 메모리 주소를 얻음
2. **\* (역참조 연산자)** = 포인터가 가리키는 주소의 값을 얻음
3. **포인터 매개변수** = 주소를 받아 원본 수정 가능
4. **주소 전달** = 값 전달보다 효율적이고 유연함
5. **주의** = 포인터를 잘못 사용하면 메모리 손상 가능
:::

---

**작성일:** 2024.01.16  
**난이도:** ⭐⭐⭐ 중급