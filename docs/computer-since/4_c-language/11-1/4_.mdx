---
title: "함수 원형과 코드 구조"
description: "함수 원형(프로토타입)의 개념과 메인 함수 배치 전략을 배워보세요"
slug: "c-function-prototype-structure"
sidebar_position: 4
date: "2024-01-15"
---

# 함수 원형과 코드 구조

## 🎯 함수 원형(Prototype)이란?

함수를 사용하기 전에 함수의 **기본 정보를 먼저 알려주는 것**을 함수 원형이라고 합니다.

### 함수 원형의 형식

```c
반환형 함수이름(매개변수);
//                     ↑
//            세미콜론(;)이 붙음 (매우 중요!)
```

### 예제

```c
// 함수 원형 (함수가 있다는 것을 컴파일러에게 알려줌)
int add(int a, int b);

int main() {
    int result = add(5, 3);  // 함수 호출 가능
    return 0;
}

// 실제 함수 정의 (함수가 하는 일을 구현)
int add(int a, int b) {
    return a + b;
}
```

## 🏗️ 함수 없이 코드를 쓸 때의 문제

함수 원형이 왜 필요한지 이해하기 위해, 문제상황을 보겠습니다.

### ❌ 잘못된 코드 (함수 원형 없이)

```c
#include <stdio.h>

int main() {
    int result = add(5, 3);  // ❌ 에러! add 함수를 모르는데?
    printf("%d\n", result);
    return 0;
}

// add 함수 정의 (main 이후에 정의)
int add(int a, int b) {
    return a + b;
}
```

**문제:** 위에서 add를 호출했는데, 아래에서 정의했으므로 컴파일러가 add가 뭔지 몰라서 **에러 발생**!

## ✅ 올바른 구조: 함수 원형 사용

### 방법 1: 함수 정의를 먼저 하기

```c
#include <stdio.h>

// 함수 정의 (전체 함수를 먼저 적음)
int add(int a, int b) {
    return a + b;
}

int main() {
    int result = add(5, 3);  // ✅ 에러 없음
    printf("%d\n", result);
    return 0;
}
```

**장점:** 간단함
**단점:** 함수가 많으면 main이 아래로 밀려남

### 방법 2: 함수 원형 사용 (✅ 권장)

```c
#include <stdio.h>

// 함수 원형 선언 (컴파일러에게 미리 알려줌)
int add(int a, int b);

int main() {
    int result = add(5, 3);  // ✅ add가 있다는 것을 알고 있음
    printf("%d\n", result);
    return 0;
}

// 함수 정의 (실제 구현)
int add(int a, int b) {
    return a + b;
}
```

**장점:** 
- main 함수가 상단에 있어 프로그램의 흐름을 파악하기 쉬움
- 함수가 많아도 구조가 깔끔함

**단점:** 함수 원형을 따로 작성해야 함

## 📝 함수 원형 작성 규칙

### 함수 원형과 함수 정의의 비교

```c
// 함수 원형 (선언)
int add(int a, int b);
//                    ↑
//              세미콜론 필수

// 함수 정의 (구현)
int add(int a, int b) {
//                    ↑
//            중괄호 {} 사용
    return a + b;
}
```

### 중요한 규칙

**1. 반환형은 정확히 같아야 함**
```c
// 원형
int add(int a, int b);

// 정의 - ✅ 같음
int add(int a, int b) {
    return a + b;
}

// 정의 - ❌ 다름! (원형은 int인데 float?)
float add(int a, int b) {  // 에러!
    return a + b;
}
```

**2. 함수 이름은 정확히 같아야 함**
```c
// 원형
int add(int a, int b);

// 정의 - ✅ 같음
int add(int a, int b) {
    return a + b;
}

// 정의 - ❌ 다름! (addition이 아니라 add여야 함)
int addition(int a, int b) {  // 에러!
    return a + b;
}
```

**3. 매개변수의 자료형은 같아야 함**
```c
// 원형
int add(int a, int b);

// 정의 - ✅ 같음
int add(int a, int b) {
    return a + b;
}

// 정의 - ❌ 다름! (float가 아니라 int여야 함)
int add(float a, float b) {  // 에러!
    return a + b;
}
```

**4. 매개변수의 이름은 달라도 괜찮음**
```c
// 원형
int add(int a, int b);

// 정의 - ✅ 괜찮음 (이름만 다름)
int add(int x, int y) {
    return x + y;
}

// 또는 원형에 이름 생략 가능
int add(int, int);  // ✅ 이렇게도 가능

// 정의
int add(int a, int b) {
    return a + b;
}
```

## 🎬 실제 프로그램 구조

### 프로그램의 전체 구조

```c
#include <stdio.h>

// ========== [1] 함수 원형들 ==========
int add(int a, int b);
void print_message();
int calculate_age(int birth_year);

// ========== [2] main 함수 ==========
int main() {
    // 여기에 프로그램의 주요 로직이 들어감
    int result = add(10, 20);
    printf("합: %d\n", result);
    
    print_message();
    
    int age = calculate_age(2000);
    printf("나이: %d\n", age);
    
    return 0;
}

// ========== [3] 함수 정의들 ==========
int add(int a, int b) {
    return a + b;
}

void print_message() {
    printf("안녕하세요!\n");
}

int calculate_age(int birth_year) {
    return 2024 - birth_year;
}
```

**구조 설명:**

1️⃣ **함수 원형들** (맨 위)
- 모든 함수의 원형을 먼저 선언
- 프로그램이 시작되기 전에 컴파일러가 읽음

2️⃣ **main 함수** (중간)
- 프로그램의 실행 흐름이 명확하게 보임
- 어떤 함수들이 호출되는지 한눈에 파악 가능

3️⃣ **함수 정의들** (아래쪽)
- 각 함수의 실제 구현
- main에서 호출되는 순서와 무관하게 배치 가능

## 🔄 컴파일러의 작동 과정

### 함수 원형이 있을 때

```
프로그램 실행 시:

[1단계] 함수 원형들을 읽음
        "아, add 함수가 있구나! int 반환, int 2개 받음"
        
[2단계] main 함수 실행
        add(10, 20) 호출
        "add는 있으니까 실행해도 되겠네"
        
[3단계] add 함수 실행
        계산 후 결과 반환
        
[4단계] main으로 돌아와서 계속 실행
```

## 🎯 프로그램 작성의 3단계

프로그래밍을 할 때 다음 순서로 작성하세요:

### 1단계: 함수 원형 선언

```c
#include <stdio.h>

// 필요한 함수들의 원형을 모두 적기
int add(int a, int b);
int subtract(int a, int b);
int multiply(int a, int b);
```

### 2단계: main 함수 작성

```c
int main() {
    // 프로그램의 전체 흐름을 작성
    // 함수들을 호출
    
    int a = 10, b = 5;
    
    printf("더하기: %d\n", add(a, b));
    printf("빼기: %d\n", subtract(a, b));
    printf("곱하기: %d\n", multiply(a, b));
    
    return 0;
}
```

### 3단계: 함수들을 정의

```c
int add(int a, int b) {
    return a + b;
}

int subtract(int a, int b) {
    return a - b;
}

int multiply(int a, int b) {
    return a * b;
}
```

## 📚 완성된 프로그램 예제

```c
#include <stdio.h>

// ===== 함수 원형 =====
int add(int a, int b);
int subtract(int a, int b);

// ===== main 함수 =====
int main() {
    int num1 = 15;
    int num2 = 8;
    
    int sum = add(num1, num2);
    int diff = subtract(num1, num2);
    
    printf("더하기: %d + %d = %d\n", num1, num2, sum);
    printf("빼기: %d - %d = %d\n", num1, num2, diff);
    
    return 0;
}

// ===== 함수 정의 =====
int add(int a, int b) {
    // 두 수를 더함
    return a + b;
}

int subtract(int a, int b) {
    // 첫 번째 수에서 두 번째 수를 뺌
    return a - b;
}
```

**실행 결과:**
```
더하기: 15 + 8 = 23
빼기: 15 - 8 = 7
```

## 📌 핵심 정리

:::note 꼭 기억하세요!
1. **함수 원형** = 함수가 있다는 것을 미리 알려주기
2. **세미콜론(;)** = 함수 원형에는 반드시 필요
3. **코드 구조** = 함수 원형 → main → 함수 정의
4. **main을 상단에 배치** = 프로그램 흐름이 명확함
5. **일관성** = 원형과 정의의 반환형, 이름, 매개변수가 같아야 함
:::

## 💡 왜 이 구조를 사용할까?

1. **가독성** - main 함수를 봐서 프로그램이 뭘 하는지 알 수 있음
2. **유지보수** - 함수를 수정해도 main에는 영향 없음
3. **확장성** - 새로운 함수를 쉽게 추가할 수 있음
4. **전문성** - 프로답게 보이는 코드 작성!

## 🎯 다음 단계

이제 프로그램의 전체 구조를 알았습니다. 다음에는 **실제로 여러 함수를 만들어서 프로그램을 작성**해봅시다!

---

**작성일:** 2024.01.15  
**난이도:** ⭐⭐ 초급