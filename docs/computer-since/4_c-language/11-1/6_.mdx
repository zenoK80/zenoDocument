---
title: "함수 실습 예제"
description: "다양한 난이도의 함수 예제를 직접 따라하며 배워보세요"
slug: "c-function-practice-examples"
sidebar_position: 6
date: "2024-01-15"
---

# 함수 실습 예제

## 📝 예제 1: 기본 덧셈 함수 (쉬운 난이도)

### 문제

두 수를 받아서 더한 결과를 반환하는 함수를 만들어보세요.

### 풀이

```c
#include <stdio.h>

// [1] 함수 원형 선언
int add(int a, int b);

// [2] main 함수
int main() {
    int num1 = 10;
    int num2 = 20;
    
    // [3] 함수 호출
    int result = add(num1, num2);
    
    // [4] 결과 출력
    printf("10 + 20 = %d\n", result);
    
    return 0;
}

// [5] 함수 정의
int add(int a, int b) {
    // 두 수를 더함
    return a + b;
}
```

### 코드 분석 (한 줄씩)

```c
int add(int a, int b);
// ↑ 반환형: int (정수를 반환)
//      ↑ 함수명: add (더하기)
//           ↑ 매개변수: int a, int b (두 개의 정수를 받음)
//                                  ↑ 세미콜론 (함수 원형에 필수!)
```

```c
int result = add(num1, num2);
// ↑ result: 반환값을 저장할 변수
//            ↑ add 함수 호출
//                     ↑ num1(10), num2(20)를 인수로 전달
```

```c
return a + b;
// ↑ return: 함수를 종료하고 값 반환
//        ↑ a + b: 10 + 20 = 30을 반환
```

### 실행 결과

```
10 + 20 = 30
```

---

## 📝 예제 2: 학생 정보 출력 함수 (쉬운 난이도)

### 문제

학생의 이름과 학번을 받아서 화면에 출력하는 함수를 만들어보세요.

### 풀이

```c
#include <stdio.h>

// [1] 함수 원형
void print_student(char name[], int student_id);

// [2] main 함수
int main() {
    // [3] 함수 호출 (여러 학생)
    print_student("김철수", 20241001);
    print_student("이영희", 20241002);
    print_student("박민준", 20241003);
    
    return 0;
}

// [4] 함수 정의
void print_student(char name[], int student_id) {
    // 받은 정보를 출력
    printf("이름: %s\n", name);
    printf("학번: %d\n", student_id);
    printf("\n");
}
```

### 코드 분석

```c
void print_student(char name[], int student_id);
// ↑ void: 반환값이 없음 (단순 출력만 함)
//      ↑ print_student: 함수명
//                       ↑ char name[]: 문자열(이름) 받음
//                                  ↑ int student_id: 정수(학번) 받음
```

```c
print_student("김철수", 20241001);
// ↑ 함수 호출
//            ↑ "김철수": name 매개변수에 전달
//                        ↑ 20241001: student_id 매개변수에 전달
```

### 실행 결과

```
이름: 김철수
학번: 20241001

이름: 이영희
학번: 20241002

이름: 박민준
학번: 20241003

```

---

## 📝 예제 3: 두 수 중 큰 수 찾기 (중간 난이도)

### 문제

두 수를 받아서 큰 수를 반환하는 함수를 만들어보세요.

### 풀이

```c
#include <stdio.h>

// [1] 함수 원형
int get_max(int a, int b);

// [2] main 함수
int main() {
    int num1 = 15;
    int num2 = 8;
    
    // [3] 함수 호출
    int bigger = get_max(num1, num2);
    
    // [4] 결과 출력
    printf("%d와 %d 중 큰 수: %d\n", num1, num2, bigger);
    
    return 0;
}

// [5] 함수 정의 (조건문 사용)
int get_max(int a, int b) {
    // a가 b보다 크면 a 반환
    if (a > b) {
        return a;
    }
    // 아니면 b 반환
    else {
        return b;
    }
}
```

### 코드 분석

```c
int get_max(int a, int b) {
    if (a > b) {
        // ↑ a가 b보다 크면
        return a;
        // ↑ a를 반환 (함수 종료)
    }
    else {
        // ↑ 그 외의 경우 (a ≤ b)
        return b;
        // ↑ b를 반환 (함수 종료)
    }
}
```

**더 짧은 버전 (3항 연산자 사용):**

```c
int get_max(int a, int b) {
    return (a > b) ? a : b;
    // ↑ a > b가 참이면 a, 거짓이면 b 반환
}
```

### 실행 결과

```
15와 8 중 큰 수: 15
```

---

## 📝 예제 4: 나이 계산 함수 (중간 난이도)

### 문제

태어난 연도를 받아서 나이를 계산해서 반환하는 함수를 만들어보세요.

### 풀이

```c
#include <stdio.h>

// [1] 함수 원형
int calculate_age(int birth_year);

// [2] main 함수
int main() {
    int age1 = calculate_age(2000);  // 2000년생
    int age2 = calculate_age(2005);  // 2005년생
    int age3 = calculate_age(1995);  // 1995년생
    
    printf("2000년생 나이: %d\n", age1);
    printf("2005년생 나이: %d\n", age2);
    printf("1995년생 나이: %d\n", age3);
    
    return 0;
}

// [3] 함수 정의
int calculate_age(int birth_year) {
    int current_year = 2024;
    int age = current_year - birth_year;
    
    return age;
}
```

### 코드 분석

```c
int age = current_year - birth_year;
// ↑ current_year(2024) - birth_year(2000) = 24

return age;
// ↑ 계산된 나이를 반환
```

### 실행 결과

```
2000년생 나이: 24
2005년생 나이: 19
1995년생 나이: 29
```

---

## 📝 예제 5: 성적 등급 판정 함수 (중간 난이도)

### 문제

점수(0~100)를 받아서 등급('A', 'B', 'C', 'D', 'F')을 반환하는 함수를 만들어보세요.

### 풀이

```c
#include <stdio.h>

// [1] 함수 원형
char get_grade(int score);

// [2] main 함수
int main() {
    int scores[] = {95, 85, 75, 65, 45};
    
    printf("점수와 등급:\n");
    
    // [3] 여러 번 함수 호출
    for (int i = 0; i < 5; i++) {
        char grade = get_grade(scores[i]);
        printf("점수 %d → 등급 %c\n", scores[i], grade);
    }
    
    return 0;
}

// [4] 함수 정의 (여러 조건 판단)
char get_grade(int score) {
    // 90점 이상
    if (score >= 90) {
        return 'A';
    }
    // 80점 이상
    else if (score >= 80) {
        return 'B';
    }
    // 70점 이상
    else if (score >= 70) {
        return 'C';
    }
    // 60점 이상
    else if (score >= 60) {
        return 'D';
    }
    // 60점 미만
    else {
        return 'F';
    }
}
```

### 코드 분석

```c
char get_grade(int score) {
    // ↑ 반환형이 char (문자 하나를 반환)
    //    score 매개변수 (점수)를 받음
    
    if (score >= 90) {
        return 'A';
        // ↑ 조건이 참이면 'A' 반환하고 함수 종료
    }
    else if (score >= 80) {
        return 'B';
        // ↑ 위 조건이 거짓이고 이 조건이 참이면 'B' 반환
    }
    // ... (계속)
}
```

### 실행 결과

```
점수와 등급:
점수 95 → 등급 A
점수 85 → 등급 B
점수 75 → 등급 C
점수 65 → 등급 D
점수 45 → 등급 F
```

---

## 📝 예제 6: 합계 계산 함수 (어려운 난이도)

### 문제

3개의 점수를 받아서 합계를 계산해서 반환하는 함수를 만들어보세요.

### 풀이

```c
#include <stdio.h>

// [1] 함수 원형
int sum_scores(int korean, int math, int english);

// [2] main 함수
int main() {
    int student1_korean = 85;
    int student1_math = 90;
    int student1_english = 88;
    
    // [3] 함수 호출
    int total = sum_scores(student1_korean, student1_math, student1_english);
    
    // [4] 결과 출력
    printf("국어: %d점\n", student1_korean);
    printf("수학: %d점\n", student1_math);
    printf("영어: %d점\n", student1_english);
    printf("합계: %d점\n", total);
    
    return 0;
}

// [5] 함수 정의 (매개변수 3개)
int sum_scores(int korean, int math, int english) {
    int total = korean + math + english;
    
    return total;
}
```

### 코드 분석

```c
int sum_scores(int korean, int math, int english);
// ↑ 매개변수가 3개 (한국어, 수학, 영어 점수)

int total = sum_scores(85, 90, 88);
// ↑ 인수도 3개를 전달해야 함 (규칙: 매개변수 개수 = 인수 개수)

int total = korean + math + english;
// ↑ 3개의 점수를 모두 더함
// 85 + 90 + 88 = 263

return total;
// ↑ 합계 263을 반환
```

### 실행 결과

```
국어: 85점
수학: 90점
영어: 88점
합계: 263점
```

---

## 📝 예제 7: 평균 계산 함수 (어려운 난이도)

### 문제

3개의 점수를 받아서 평균을 계산해서 반환하는 함수를 만들어보세요. (소수점 포함)

### 풀이

```c
#include <stdio.h>

// [1] 함수 원형 (반환형이 double - 실수)
double calculate_average(int korean, int math, int english);

// [2] main 함수
int main() {
    // [3] 함수 호출
    double avg = calculate_average(85, 90, 88);
    
    // [4] 결과 출력
    printf("평균: %.2f점\n", avg);
    //         ↑ %.2f: 소수점 이하 2자리까지 출력
    
    return 0;
}

// [5] 함수 정의 (반환형이 double)
double calculate_average(int korean, int math, int english) {
    double sum = korean + math + english;
    //      ↑ double로 타입 캐스팅
    
    double average = sum / 3.0;
    //                      ↑ 3.0으로 나누어 실수 계산
    
    return average;
}
```

### 코드 분석

```c
double calculate_average(...) {
// ↑ 반환형이 double (실수를 반환)
// (소수점이 필요하기 때문)

double sum = korean + math + english;
// ↑ double로 선언하여 정수를 실수로 처리

double average = sum / 3.0;
// ↑ 3.0 (3의 실수형)으로 나누어 소수점 계산
// 263 ÷ 3.0 = 87.666...
```

### 실행 결과

```
평균: 87.67점
```

---

## 📝 예제 8: 여러 함수를 조합하는 프로그램 (종합)

### 문제

학생의 3개 과목 점수를 입력받아, 합계와 평균, 그리고 등급을 판정하는 프로그램을 만들어보세요.

### 풀이

```c
#include <stdio.h>

// [1] 함수 원형들
int sum_scores(int a, int b, int c);
double calculate_average(int a, int b, int c);
char get_grade(double average);

// [2] main 함수
int main() {
    int korean = 85;
    int math = 90;
    int english = 88;
    
    printf("=== 성적 조회 ===\n");
    printf("국어: %d점\n", korean);
    printf("수학: %d점\n", math);
    printf("영어: %d점\n", english);
    printf("\n");
    
    // [3] 합계 계산
    int total = sum_scores(korean, math, english);
    printf("합계: %d점\n", total);
    
    // [4] 평균 계산
    double avg = calculate_average(korean, math, english);
    printf("평균: %.2f점\n", avg);
    
    // [5] 등급 판정
    char grade = get_grade(avg);
    printf("등급: %c\n", grade);
    
    return 0;
}

// [6] 함수 정의들

// 합계 계산
int sum_scores(int a, int b, int c) {
    return a + b + c;
}

// 평균 계산
double calculate_average(int a, int b, int c) {
    double sum = a + b + c;
    return sum / 3.0;
}

// 등급 판정
char get_grade(double average) {
    if (average >= 90) {
        return 'A';
    }
    else if (average >= 80) {
        return 'B';
    }
    else if (average >= 70) {
        return 'C';
    }
    else if (average >= 60) {
        return 'D';
    }
    else {
        return 'F';
    }
}
```

### 실행 결과

```
=== 성적 조회 ===
국어: 85점
수학: 90점
영어: 88점

합계: 263점
평균: 87.67점
등급: B
```

---

## 📌 실습 팁

### ✅ 잘된 함수의 특징

```c
1. 한 가지 기능만 한다
   ✅ int add(int a, int b)  // 더하기만 함
   ❌ int add_and_print(int a, int b)  // 두 가지를 함

2. 함수명이 명확하다
   ✅ int calculate_age()
   ❌ int age()

3. 반환값이 명확하다
   ✅ int get_sum()  // 합계를 정수로 반환
   ❌ void get_sum()  // void? 뭘 반환하는데?
```

### 🎯 함수 작성 순서

```
1. 함수가 뭘 할건지 생각하기
2. 함수 원형 작성
3. main 함수에서 함수 호출하기
4. 함수 정의 작성
5. 테스트
```

## 🎓 다음 단계

실습 예제들을 완벽히 이해했다면, 여러분도 **자신만의 함수를 만들 수 있습니다**!

더 복잡한 프로그램에서도 함수의 원칙은 같습니다:
- 함수 원형 선언
- 함수 호출
- 함수 정의

이 3가지만 기억하면 됩니다!

---

**작성일:** 2024.01.15  
**난이도:** ⭐⭐⭐ 초급~중급