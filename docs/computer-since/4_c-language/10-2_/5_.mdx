---
title: "포인터 실습 예제"
description: "실제 프로그래밍에서 포인터를 활용하는 다양한 예제를 통해 실력을 향상시킵니다."
slug: "pointer-examples"
sidebar_position: 5
---

# 포인터 실습 예제

## 학습 목표
- 일반 변수와 포인터의 관계를 실습합니다
- 포인터를 이용한 값 교환 프로그램을 만듭니다
- 문자형 배열과 포인터를 응용합니다

---

## 1. 기본 예제: 변수와 포인터

### 예제 1-1: 간단한 포인터 사용

```c
#include <stdio.h>

int main() {
    char ch = 'A';           // 문자 변수 선언
    char *p = &ch;           // 포인터 선언 및 초기화
    
    printf("ch의 값: %c\n", ch);
    printf("ch의 주소: %p\n", &ch);
    printf("p의 값: %p\n", p);
    printf("p가 가리키는 값: %c\n", *p);
    
    return 0;
}
```

### 코드 해설 (라인별)

```c
char ch = 'A';
// [라인 1] 문자형 변수 ch 선언
// 'A'로 초기화
// 메모리: 어딘가(예: 0x12)에 'A' 저장

char *p = &ch;
// [라인 2] 문자형 포인터 p 선언
// &ch: ch의 주소를 가져옴
// p에 ch의 주소(0x12) 저장

printf("ch의 값: %c\n", ch);
// [라인 3] ch에 저장된 문자 출력
// %c: 문자 형식으로 출력
// 결과: A

printf("ch의 주소: %p\n", &ch);
// [라인 4] ch의 메모리 주소 출력
// &ch: ch의 주소
// %p: 주소 형식으로 출력
// 결과: 0x7ffd5467e8c4 (예시)

printf("p의 값: %p\n", p);
// [라인 5] p에 저장된 주소 출력
// p는 ch의 주소를 저장하고 있음
// 결과: 0x7ffd5467e8c4 (ch의 주소와 동일)

printf("p가 가리키는 값: %c\n", *p);
// [라인 6] p가 가리키는 위치의 값 출력
// *p: p가 가리키는 곳(0x12)의 값
// 결과: A
```

### 실행 결과

```
ch의 값: A
ch의 주소: 0x7ffd5467e8c4
p의 값: 0x7ffd5467e8c4
p가 가리키는 값: A
```

### 메모리 상태

```
[변수 ch]
주소: 0x7ffd5467e8c4
┌─────┐
│  A  │  ← ch에 저장된 값
└─────┘
  ↑
  │
[포인터 p]
주소: 0x7ffd5467e900
┌──────────────────┐
│ 0x7ffd5467e8c4   │  ← ch의 주소 저장
└──────────────────┘
```

:::tip 주소는 매번 달라요!
프로그램을 실행할 때마다 주소가 달라집니다.
중요한 것은 **p와 &ch가 같은 값**이라는 점입니다!
:::

---

## 2. 포인터로 값 변경하기

### 예제 2-1: 간접 참조로 값 수정

```c
#include <stdio.h>

int main() {
    char ch = 'A';           // 초기값: A
    char *p1 = &ch;          // p1은 ch를 가리킴
    char *p2 = p1;           // p2도 ch를 가리킴
    
    *p2 = 'Z';               // p2를 통해 ch의 값 변경
    
    printf("ch의 값: %c\n", ch);     // Z 출력
    
    return 0;
}
```

### 코드 해설 (단계별)

```c
// [단계 1] 변수 선언 및 초기화
char ch = 'A';
// ch를 'A'로 초기화
// 메모리: 0x12 주소에 'A' 저장

// [단계 2] 첫 번째 포인터 선언
char *p1 = &ch;
// p1에 ch의 주소(0x12) 저장
// p1은 ch를 가리킴

// [단계 3] 두 번째 포인터 선언
char *p2 = p1;
// p2 = p1의 값
// p1이 이미 포인터이므로 & 불필요
// p2도 ch의 주소(0x12)를 저장
// p1과 p2 모두 ch를 가리킴

// [단계 4] 값 변경
*p2 = 'Z';
// *p2: p2가 가리키는 곳(0x12)으로 이동
// = 'Z': 그 위치에 'Z' 저장
// 결과: ch의 값이 'A' → 'Z'로 변경

// [단계 5] 결과 확인
printf("ch의 값: %c\n", ch);
// ch를 출력하면 'Z'가 나옴
```

### 메모리 변화 과정

```
[초기 상태]
ch (0x12):  │ A │
p1 (0x20):  │0x12│ ─┐
p2 (0x30):  │0x12│ ─┴→ 둘 다 ch를 가리킴

[*p2 = 'Z' 실행 후]
ch (0x12):  │ Z │  ← 값이 변경됨!
p1 (0x20):  │0x12│ ─┐
p2 (0x30):  │0x12│ ─┴→ 여전히 ch를 가리킴
```

### 실행 결과

```
ch의 값: Z
```

:::warning 포인터 체인
p2 → p1 → ch
p2를 통해 간접적으로 ch의 값을 변경할 수 있습니다!
:::

---

## 3. 두 값 교환하기 (Swap)

### 예제 3-1: 포인터를 이용한 값 교환

```c
#include <stdio.h>

int main() {
    int a, b;                // 교환할 변수
    int *p1, *p2;            // 포인터 변수
    int temp;                // 임시 저장 변수
    
    // 값 입력
    printf("첫 번째 숫자: ");
    scanf("%d", &a);
    printf("두 번째 숫자: ");
    scanf("%d", &b);
    
    // 포인터에 주소 저장
    p1 = &a;
    p2 = &b;
    
    // 값 교환
    temp = *p1;       // temp = a의 값
    *p1 = *p2;        // a = b의 값
    *p2 = temp;       // b = temp의 값
    
    // 결과 출력
    printf("교환 후 a = %d, b = %d\n", a, b);
    
    return 0;
}
```

### 코드 해설 (단계별)

```c
// [선언부]
int a, b;
// 교환할 두 정수형 변수 선언

int *p1, *p2;
// 정수형 포인터 두 개 선언
// 각각 a와 b의 주소를 저장할 예정

int temp;
// 값을 교환할 때 임시로 저장할 변수

// [입력부]
scanf("%d", &a);
// 사용자로부터 정수 입력
// &a: a의 주소에 값 저장
// 예: 3 입력

scanf("%d", &b);
// 두 번째 정수 입력
// &b: b의 주소에 값 저장
// 예: 5 입력

// [포인터 설정부]
p1 = &a;
// p1에 a의 주소 저장
// p1은 이제 a를 가리킴

p2 = &b;
// p2에 b의 주소 저장
// p2는 이제 b를 가리킴

// [교환부]
temp = *p1;
// 1단계: p1이 가리키는 값(a의 값)을 temp에 저장
// temp = 3

*p1 = *p2;
// 2단계: p2가 가리키는 값(b의 값)을 p1이 가리키는 곳에 저장
// a = 5

*p2 = temp;
// 3단계: temp에 저장된 값을 p2가 가리키는 곳에 저장
// b = 3
```

### 교환 과정 시각화

```
초기 상태:
a = 3    b = 5    temp = ?
p1 → a   p2 → b

[1단계] temp = *p1;
a = 3    b = 5    temp = 3
p1 → a   p2 → b   ↑
      └─────────────┘ (a의 값 복사)

[2단계] *p1 = *p2;
a = 5    b = 5    temp = 3
p1 → a   p2 → b
      ↑       │
      └───────┘ (b의 값을 a에 복사)

[3단계] *p2 = temp;
a = 5    b = 3    temp = 3
p1 → a   p2 → b   │
              ↑   │
              └───┘ (temp의 값을 b에 복사)

최종 결과:
a = 5    b = 3
```

### 실행 예시

```
첫 번째 숫자: 3
두 번째 숫자: 5
교환 후 a = 5, b = 3
```

:::tip 왜 포인터를 사용하나요?
```c
// 포인터 없이 (일반적인 방법)
temp = a;
a = b;
b = temp;

// 포인터 사용
temp = *p1;
*p1 = *p2;
*p2 = temp;
```
포인터를 사용하면 **함수로 만들 때** 더 유용합니다!
함수에서 원본 값을 직접 변경할 수 있기 때문입니다.
:::

---

## 4. 문자열과 포인터 응용

### 예제 4-1: 문자열 부분 출력

```c
#include <stdio.h>

int main() {
    char s[8] = "BASIC-C";
    char *p = s;
    
    printf("전체 문자열: %s\n", s);
    printf("3번째부터: %s\n", s+3);
    printf("3번째 문자: %c\n", s[3]);
    printf("포인터 사용: %s\n", p+3);
    printf("값 참조: %c\n", *(p+3));
    
    return 0;
}
```

### 코드 해설 (라인별)

```c
char s[8] = "BASIC-C";
// 크기 8인 문자 배열 선언
// "BASIC-C" 저장 (7글자 + 널문자)
// 메모리: B A S I C - C \0

char *p = s;
// 포인터 p에 배열 s의 주소 저장
// p는 s[0]의 주소를 가리킴

printf("전체 문자열: %s\n", s);
// %s: 문자열 출력
// s: 배열의 시작 주소부터 널문자까지
// 결과: BASIC-C

printf("3번째부터: %s\n", s+3);
// s+3: 3번 인덱스의 주소
// %s로 출력하면 그 위치부터 끝까지
// 결과: IC-C

printf("3번째 문자: %c\n", s[3]);
// s[3]: 3번 인덱스의 값
// %c: 문자 하나만 출력
// 결과: I

printf("포인터 사용: %s\n", p+3);
// p+3: 포인터 연산 (3칸 이동)
// %s: 문자열 출력
// 결과: IC-C

printf("값 참조: %c\n", *(p+3));
// *(p+3): 3번 위치의 값 참조
// %c: 문자 하나만 출력
// 결과: I
```

### 메모리 상태

```
배열 s:
인덱스:  [0]  [1]  [2]  [3]  [4]  [5]  [6]  [7]
주소:   0x12 0x13 0x14 0x15 0x16 0x17 0x18 0x19
      ┌────┬────┬────┬────┬────┬────┬────┬────┐
값:   │ B  │ A  │ S  │ I  │ C  │ -  │ C  │ \0 │
      └────┴────┴────┴────┴────┴────┴────┴────┘
      ↑                   ↑
      s                   s+3 (또는 p+3)
      (0x12)              (0x15)
```

### 실행 결과

```
전체 문자열: BASIC-C
3번째부터: IC-C
3번째 문자: I
포인터 사용: IC-C
값 참조: I
```

:::note %c vs %s 차이
```c
%c, s[3]    → I      (문자 하나)
%s, s+3     → IC-C   (문자열 전체)
```
형식 지정자에 따라 출력 방식이 달라집니다!
:::

---

## 5. 종합 실습 예제

### 예제 5-1: 포인터 완전 정복

```c
#include <stdio.h>

int main() {
    // 변수 선언
    int num = 100;
    int *ptr1 = NULL;
    int *ptr2 = NULL;
    
    printf("=== 초기 상태 ===\n");
    printf("num = %d\n", num);
    
    // 포인터 할당
    ptr1 = &num;
    ptr2 = ptr1;
    
    printf("\n=== 포인터 할당 후 ===\n");
    printf("num의 주소: %p\n", &num);
    printf("ptr1의 값: %p\n", ptr1);
    printf("ptr2의 값: %p\n", ptr2);
    printf("*ptr1 = %d\n", *ptr1);
    printf("*ptr2 = %d\n", *ptr2);
    
    // 포인터로 값 변경
    *ptr1 = 200;
    
    printf("\n=== *ptr1 = 200 후 ===\n");
    printf("num = %d\n", num);
    printf("*ptr1 = %d\n", *ptr1);
    printf("*ptr2 = %d\n", *ptr2);
    
    // 다른 포인터로 값 변경
    *ptr2 = 300;
    
    printf("\n=== *ptr2 = 300 후 ===\n");
    printf("num = %d\n", num);
    printf("*ptr1 = %d\n", *ptr1);
    printf("*ptr2 = %d\n", *ptr2);
    
    return 0;
}
```

### 코드 실행 흐름

```c
// [1단계] 초기화
int num = 100;
int *ptr1 = NULL;
int *ptr2 = NULL;
// num: 100 저장
// ptr1, ptr2: NULL (아무것도 가리키지 않음)

// [2단계] 포인터 할당
ptr1 = &num;
ptr2 = ptr1;
// ptr1: num의 주소 저장
// ptr2: ptr1의 값(num의 주소) 복사
// 둘 다 num을 가리킴

// [3단계] 첫 번째 변경
*ptr1 = 200;
// ptr1이 가리키는 곳(num)에 200 저장
// num: 100 → 200

// [4단계] 두 번째 변경
*ptr2 = 300;
// ptr2가 가리키는 곳(num)에 300 저장
// num: 200 → 300
```

### 실행 결과

```
=== 초기 상태 ===
num = 100

=== 포인터 할당 후 ===
num의 주소: 0x7ffd5467e8c4
ptr1의 값: 0x7ffd5467e8c4
ptr2의 값: 0x7ffd5467e8c4
*ptr1 = 100
*ptr2 = 100

=== *ptr1 = 200 후 ===
num = 100
*ptr1 = 200
*ptr2 = 200

=== *ptr2 = 300 후 ===
num = 300
*ptr1 = 300
*ptr2 = 300
```

### 핵심 개념

```
ptr1과 ptr2는 같은 곳을 가리킴
     ↓
둘 중 하나로 값을 변경하면
     ↓
num의 값이 변경됨
     ↓
모든 포인터가 변경된 값을 봄
```

---

## 6. 디버깅 팁

### 자주 하는 실수

```c
// 실수 1: 초기화하지 않은 포인터 사용
int *p;
*p = 10;     // ❌ 위험! p가 어디를 가리키는지 모름

// 올바른 방법
int *p = NULL;
int num = 0;
p = &num;
*p = 10;     // ✅ 안전

// 실수 2: & 연산자 오용
int *p1, *p2;
p1 = &num;
p2 = &p1;    // ❌ 타입 불일치

// 올바른 방법
p2 = p1;     // ✅ 포인터끼리는 & 없이

// 실수 3: * 연산자 빠뜨림
int *p = &num;
p = 100;     // ❌ p에 100이라는 주소 저장?

// 올바른 방법
*p = 100;    // ✅ p가 가리키는 곳에 100 저장
```

### 안전한 포인터 사용 패턴

```c
// 1. 항상 NULL로 초기화
int *ptr = NULL;

// 2. 사용 전에 확인
if (ptr != NULL) {
    *ptr = 100;
}

// 3. 주소 대입 후 사용
int num;
ptr = &num;
*ptr = 100;  // 이제 안전
```

---

## 학습 정리

### 핵심 개념 체크리스트

✅ **포인터는 항상 초기화**합니다 (NULL 또는 주소)  
✅ **& 연산자**로 주소를 얻습니다  
✅ **\* 연산자**로 값을 읽거나 수정합니다  
✅ **포인터끼리 대입**할 때는 & 없이 사용합니다  
✅ **값 교환은 temp 변수**를 활용합니다  

### 실습 패턴 정리

```c
// [패턴 1] 기본 포인터 사용
int a = 10;
int *p = &a;
printf("%d", *p);  // 값 읽기

// [패턴 2] 값 변경
*p = 20;           // 값 쓰기

// [패턴 3] 포인터 체인
int *p1 = &a;
int *p2 = p1;
*p2 = 30;          // a가 30으로 변경

// [패턴 4] 값 교환
temp = *p1;
*p1 = *p2;
*p2 = temp;
```

### 다음 학습을 위한 준비

이제 여러분은:
- ✅ 포인터의 기본 개념을 이해했습니다
- ✅ 포인터로 값을 읽고 수정할 수 있습니다
- ✅ 배열과 포인터의 관계를 알았습니다
- ✅ 실전 예제를 직접 작성할 수 있습니다

---

## 연습 문제

### 문제 1: 기본 포인터
다음 코드의 출력 결과를 예측해보세요.

```c
int x = 5;
int *p = &x;
*p = *p + 3;
printf("%d", x);
```

<details>
<summary>정답 보기</summary>

**답: 8**

해설:
- x = 5
- p는 x를 가리킴
- *p = *p + 3 → *p = 5 + 3 → *p = 8
- x의 값도 8로 변경됨
</details>

### 문제 2: 포인터 체인
다음 코드의 출력 결과를 예측해보세요.

```c
int a = 10;
int *p1 = &a;
int *p2 = p1;
*p1 = 20;
printf("%d %d", a, *p2);
```

<details>
<summary>정답 보기</summary>

**답: 20 20**

해설:
- a = 10
- p1과 p2 모두 a를 가리킴
- *p1 = 20으로 a의 값 변경
- a와 *p2 모두 20
</details>

---

축하합니다! 🎉 
포인터의 기초를 모두 마스터하셨습니다!
이제 더 복잡한 프로그램을 만들 준비가 되었습니다! 🚀