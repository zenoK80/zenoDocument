---
title: "포인터 연산자"
description: "포인터를 사용하여 값을 읽고 수정하는 방법을 배웁니다."
slug: "pointer-operators"
sidebar_position: 3
---

# 포인터 연산자

## 학습 목표
- & 연산자(주소 연산자)를 이해합니다
- * 연산자(간접 참조 연산자)를 이해합니다
- 포인터로 값을 읽고 수정하는 방법을 배웁니다

---

## 1. 포인터 연산자의 종류

C언어에서 포인터와 함께 사용하는 두 가지 주요 연산자가 있습니다.

### 연산자 정리표

| 연산자 | 이름 | 기능 | 예시 |
|--------|------|------|------|
| `&` | 주소 연산자 | 변수의 주소를 가져옴 | `&n` |
| `*` | 간접 참조 연산자 | 포인터가 가리키는 값을 가져옴/수정 | `*ptr` |

---

## 2. & 연산자 (주소 연산자)

### 기본 개념

**& 연산자**는 변수의 **메모리 주소**를 반환합니다.

```c
int a = 10;
printf("%p", &a);  // a의 주소를 출력
```

### 상세 예제

```c
#include <stdio.h>

int main() {
    int n = 100;
    
    printf("n의 값: %d\n", n);      // 값 출력
    printf("n의 주소: %p\n", &n);   // 주소 출력
    
    return 0;
}
```

### 코드 해설 (한 줄씩)

```c
int n = 100;
// int n: 정수형 변수 n 선언
// = 100: n에 100을 저장
// 메모리의 어딘가(예: 0x12)에 100이 저장됨

printf("n의 값: %d\n", n);
// n: 변수에 저장된 값 그대로 사용
// %d: 정수를 출력하는 형식 지정자
// 결과: 100

printf("n의 주소: %p\n", &n);
// &n: n의 주소를 가져옴
// %p: 포인터(주소)를 출력하는 형식 지정자
// 결과: 0x12... (실제 메모리 주소)
```

### 실행 결과

```
n의 값: 100
n의 주소: 0x7ffd5467e8c4
```

:::tip & 연산자 사용 시점
- 포인터에 주소를 저장할 때
- scanf 함수로 값을 입력받을 때
```c
int num;
scanf("%d", &num);  // num의 주소에 값을 저장
```
:::

---

## 3. * 연산자 (간접 참조 연산자)

### 기본 개념

**별표(*)** 연산자는 두 가지 의미로 사용됩니다:

1. **선언 시**: 포인터 변수임을 표시
2. **사용 시**: 포인터가 가리키는 곳의 값을 읽거나 수정

### 예제 1: 값 읽기

```c
int a = 10;
int *p = &a;

printf("%d", *p);  // p가 가리키는 값 출력 → 10
```

### 코드 해설

```c
int a = 10;
// a라는 변수에 10을 저장
// 메모리: 0x12 주소에 10이 저장됨

int *p = &a;
// p는 포인터 변수
// &a를 통해 a의 주소(0x12)를 p에 저장
// 메모리: p에는 0x12가 저장됨

printf("%d", *p);
// *p: p가 가리키는 주소(0x12)로 가서 값을 읽어옴
// 결과: 10
```

### 메모리 상태

```
[변수 a]
주소: 0x12
┌──────┐
│  10  │  ← a의 값
└──────┘
   ↑
   │
   │ p가 가리킴
   │
[포인터 p]
주소: 0x52
┌──────┐
│ 0x12 │  ← a의 주소 저장
└──────┘

*p를 사용하면:
p → 0x12 주소로 이동 → 그곳의 값(10) 읽기
```

---

## 4. 포인터로 값 수정하기

### 예제 2: 값 변경

```c
int a = 0;
int *p = &a;

*p = 1;  // p가 가리키는 곳에 1을 저장

printf("%d", a);  // 1 출력
```

### 코드 해설 (단계별)

```c
// [1단계] 변수 선언 및 초기화
int a = 0;
// a에 0을 저장
// 메모리: 0x12 주소에 0 저장

// [2단계] 포인터 선언
int *p = &a;
// p에 a의 주소(0x12) 저장
// 메모리: p에는 0x12가 저장됨

// [3단계] 포인터로 값 변경
*p = 1;
// *p: p가 가리키는 주소(0x12)로 이동
// = 1: 그 위치에 1을 저장
// 결과: a의 값이 0에서 1로 변경됨!

// [4단계] 결과 확인
printf("%d", a);
// a를 출력하면 1이 나옴
```

### 메모리 변화 과정

```
[초기 상태]
a (0x12):  │ 0 │
p (0x52):  │0x12│
            └──→ a를 가리킴

[*p = 1 실행 후]
a (0x12):  │ 1 │  ← 값이 변경됨!
p (0x52):  │0x12│
            └──→ 여전히 a를 가리킴
```

:::warning 중요한 포인트
```c
*p = 1;  // p가 가리키는 곳의 값을 변경
         // a의 값이 변경됨!

p = 1;   // ❌ 잘못된 사용!
         // p에 1이라는 주소를 저장? (의미 없음)
```
:::

---

## 5. 포인터 간의 대입

### 포인터끼리 주소 복사하기

```c
int a = 0;
int *p1 = &a;    // p1에 a의 주소 저장
int *p2 = p1;    // p2에 p1의 값(a의 주소) 복사
```

### 코드 해설

```c
int a = 0;
// 변수 a를 0으로 초기화
// 메모리: 0x12에 0 저장

int *p1 = &a;
// p1은 포인터 변수
// &a: a의 주소(0x12)를 가져옴
// p1에 0x12 저장

int *p2 = p1;
// p2는 포인터 변수
// p1: 이미 포인터이므로 &를 붙이지 않음!
// p1의 값(0x12)을 p2에 복사
```

### 메모리 상태

```
[변수 a]
주소: 0x12
┌──────┐
│  0   │
└──────┘
   ↑  ↑
   │  │
   │  └───────┐
   │          │
[포인터 p1]   [포인터 p2]
주소: 0x20    주소: 0x30
┌──────┐    ┌──────┐
│ 0x12 │    │ 0x12 │
└──────┘    └──────┘

p1과 p2 모두 a를 가리킴!
```

### 잘못된 사용 예시

```c
int a = 0;
int *p1 = &a;
int *p2 = &p1;  // ❌ 잘못됨!
```

:::danger 왜 잘못되었나요?
```c
&p1  // p1의 주소 (포인터의 주소)
     // int *p2는 int 변수의 주소를 저장하는 용도
     // 포인터의 주소를 저장하려면 이중 포인터(**) 필요

// 올바른 사용
int *p2 = p1;   // ✅ p1이 저장한 주소를 복사
```
:::

---

## 6. 종합 예제

### 완전한 예제 프로그램

```c
#include <stdio.h>

int main() {
    // 변수 선언
    int a = 10;
    int b = 20;
    
    // 포인터 선언
    int *p1 = NULL;
    int *p2 = NULL;
    
    // 주소 저장
    p1 = &a;
    p2 = &b;
    
    // 값 출력
    printf("a의 값: %d\n", a);           // 10
    printf("*p1의 값: %d\n", *p1);       // 10
    
    printf("b의 값: %d\n", b);           // 20
    printf("*p2의 값: %d\n", *p2);       // 20
    
    // 포인터로 값 변경
    *p1 = 100;
    *p2 = 200;
    
    printf("\n--- 값 변경 후 ---\n");
    printf("a의 값: %d\n", a);           // 100
    printf("b의 값: %d\n", b);           // 200
    
    return 0;
}
```

### 코드 실행 과정 (단계별)

```c
// [1단계] 변수 선언
int a = 10;
int b = 20;
// 메모리: a(0x10)에 10, b(0x20)에 20 저장

// [2단계] 포인터 선언 및 NULL 초기화
int *p1 = NULL;
int *p2 = NULL;
// p1과 p2는 아직 아무것도 가리키지 않음

// [3단계] 주소 저장
p1 = &a;  // p1에 a의 주소(0x10) 저장
p2 = &b;  // p2에 b의 주소(0x20) 저장

// [4단계] 값 출력
printf("a의 값: %d\n", a);      // a 변수의 값 → 10
printf("*p1의 값: %d\n", *p1);  // p1이 가리키는 값 → 10

// [5단계] 포인터로 값 변경
*p1 = 100;
// p1이 가리키는 주소(0x10)로 가서
// 그곳의 값을 100으로 변경
// 결과: a의 값이 10 → 100

*p2 = 200;
// p2가 가리키는 주소(0x20)로 가서
// 그곳의 값을 200으로 변경
// 결과: b의 값이 20 → 200
```

### 실행 결과

```
a의 값: 10
*p1의 값: 10
b의 값: 20
*p2의 값: 20

--- 값 변경 후 ---
a의 값: 100
b의 값: 200
```

---

## 7. 주소 저장 vs 값 변경

### 두 가지 사용 패턴

```c
int a = 10;
int *p;

// 패턴 1: 주소 저장 (& 사용)
p = &a;      // a의 주소를 p에 저장

// 패턴 2: 값 변경 (* 사용)
*p = 20;     // p가 가리키는 곳의 값을 20으로 변경
```

### 비교표

| 작업 | 코드 | 설명 | 결과 |
|------|------|------|------|
| 주소 저장 | `p = &a` | a의 주소를 p에 저장 | p에 주소 저장 |
| 주소 복사 | `p2 = p1` | p1의 주소를 p2에 복사 | 같은 곳 가리킴 |
| 값 읽기 | `x = *p` | p가 가리키는 값을 읽음 | x에 값 저장 |
| 값 변경 | `*p = 30` | p가 가리키는 곳의 값 변경 | 원본 변수 변경 |

---

## 8. 실수하기 쉬운 부분

### 실수 1: & 연산자를 잘못 사용

```c
int a = 10;
int *p1 = &a;   // ✅ 올바름
int *p2 = &p1;  // ❌ 잘못됨! (이중 포인터 필요)
int *p3 = p1;   // ✅ 올바름
```

### 실수 2: * 연산자를 빼먹음

```c
int a = 10;
int *p = &a;

p = 20;    // ❌ p에 20이라는 주소를 저장? (의미 없음)
*p = 20;   // ✅ p가 가리키는 곳에 20을 저장
```

### 실수 3: NULL 포인터 사용

```c
int *p = NULL;
*p = 10;        // ❌ 위험! NULL은 아무것도 가리키지 않음

int *p = NULL;
int a = 10;
p = &a;         // ✅ 먼저 주소를 저장
*p = 10;        // ✅ 이제 안전하게 사용 가능
```

---

## 학습 정리

### 핵심 개념 체크리스트

✅ **& 연산자**는 변수의 주소를 가져옵니다  
✅ **\* 연산자**는 포인터가 가리키는 값을 읽거나 수정합니다  
✅ **주소 저장**: `p = &a` 또는 `p = p1`  
✅ **값 변경**: `*p = 새로운값`  
✅ **포인터는 반드시 초기화** 후 사용합니다  

### 연산자 사용 패턴

```c
// [패턴 1] 일반 변수의 주소를 포인터에 저장
int a = 10;
int *p = &a;    // & 사용

// [패턴 2] 포인터를 다른 포인터에 복사
int *p2 = p;    // & 없이 사용

// [패턴 3] 포인터로 값 읽기
int x = *p;     // * 사용

// [패턴 4] 포인터로 값 변경
*p = 20;        // * 사용
```

---

## 다음 단계

이제 **배열과 포인터**가 어떻게 연결되는지 배워보겠습니다! 
배열은 사실 포인터와 아주 밀접한 관계가 있답니다! 🚀