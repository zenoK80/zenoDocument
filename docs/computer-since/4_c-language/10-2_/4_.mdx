---
title: "배열과 포인터의 관계"
description: "배열과 포인터가 어떻게 연결되어 있는지 이해하고 활용하는 방법을 배웁니다."
slug: "arrays-and-pointers"
sidebar_position: 4
---

# 배열과 포인터의 관계

## 학습 목표
- 배열 이름이 포인터라는 것을 이해합니다
- 배열과 포인터의 호환성을 학습합니다
- 포인터로 배열 요소에 접근하는 방법을 배웁니다

---

## 1. 배열 이름은 포인터입니다

### 놀라운 사실! 🎯

배열의 이름은 **첫 번째 요소의 주소**를 나타내는 **포인터**입니다!

```c
int arr[3] = {10, 20, 30};
// arr은 arr[0]의 주소를 가리키는 포인터
```

### 메모리 구조

```
배열:        arr[0]   arr[1]   arr[2]
주소:        0x100    0x104    0x108
          ┌────────┬────────┬────────┐
값:       │   10   │   20   │   30   │
          └────────┴────────┴────────┘
          ↑
          │
arr ──────┘  (arr은 0x100을 가리킴)
```

### 확인 예제

```c
#include <stdio.h>

int main() {
    int arr[3] = {10, 20, 30};
    
    printf("arr의 값: %p\n", arr);          // 배열 이름
    printf("&arr[0]의 값: %p\n", &arr[0]);  // 첫 번째 요소의 주소
    
    return 0;
}
```

### 코드 해설

```c
int arr[3] = {10, 20, 30};
// arr: 크기 3인 정수형 배열
// {10, 20, 30}: 초기값
// 메모리: 연속된 12바이트(4바이트×3) 할당

printf("arr의 값: %p\n", arr);
// arr: 배열 이름 자체가 주소
// %p: 포인터(주소) 출력 형식
// 결과: 0x100 (예시)

printf("&arr[0]의 값: %p\n", &arr[0]);
// &arr[0]: 첫 번째 요소의 주소
// 결과: 0x100 (arr과 같은 값!)
```

### 실행 결과

```
arr의 값: 0x7ffd5467e8c0
&arr[0]의 값: 0x7ffd5467e8c0
```

:::tip 핵심 포인트
```c
arr == &arr[0]  // 항상 참!
```
배열 이름은 첫 번째 요소의 주소와 같습니다.
:::

---

## 2. 배열과 포인터의 호환

### 배열을 포인터에 저장하기

배열 이름을 포인터 변수에 대입할 수 있습니다.

```c
char s[8] = "BASIC-C";
char *p = s;  // & 기호 없이 대입!
```

### 코드 해설

```c
char s[8] = "BASIC-C";
// char s[8]: 크기 8인 문자 배열 선언
// = "BASIC-C": 문자열로 초기화
// 메모리: B A S I C - C \0 저장

char *p = s;
// char *p: 문자형 포인터 p 선언
// = s: 배열 s의 주소를 p에 저장
// & 기호 불필요! s 자체가 이미 주소
```

### 메모리 상태

```
배열 s:
인덱스:   [0]   [1]   [2]   [3]   [4]   [5]   [6]   [7]
주소:    0x12  0x13  0x14  0x15  0x16  0x17  0x18  0x19
       ┌─────┬─────┬─────┬─────┬─────┬─────┬─────┬─────┐
값:    │  B  │  A  │  S  │  I  │  C  │  -  │  C  │ \0  │
       └─────┴─────┴─────┴─────┴─────┴─────┴─────┴─────┘
       ↑
       │
포인터 p: 0x12 저장 (s의 시작 주소)
```

:::warning 배열 vs 일반 변수
```c
// 일반 변수: & 필요
int a = 10;
int *p1 = &a;   // ✅ & 사용

// 배열: & 불필요
int arr[3];
int *p2 = arr;  // ✅ & 없이 사용 (arr이 이미 주소)
int *p3 = &arr; // ❌ 타입 불일치 (배열 포인터)
```
:::

---

## 3. 포인터로 배열 접근하기

### 두 가지 접근 방법

배열 요소에 접근하는 방법은 두 가지입니다:

1. **배열 표기법**: `arr[i]`
2. **포인터 표기법**: `*(arr + i)`

### 예제 코드

```c
char s[8] = "BASIC-C";
char *p = s;

// 방법 1: 배열 표기법
printf("%c\n", s[3]);     // I

// 방법 2: 포인터 표기법
printf("%c\n", *(p + 3)); // I
```

### 코드 해설 (단계별)

```c
char s[8] = "BASIC-C";
// s[0]='B', s[1]='A', s[2]='S', s[3]='I', ...
// 주소: 0x12, 0x13, 0x14, 0x15, ...

char *p = s;
// p에 s의 시작 주소(0x12) 저장

printf("%c\n", s[3]);
// s[3]: 인덱스 3번째 요소
// 주소 계산: 0x12 + 3 = 0x15
// 결과: 'I'

printf("%c\n", *(p + 3));
// p: 0x12 (시작 주소)
// p + 3: 0x12 + 3 = 0x15 (주소 계산)
// *(p + 3): 0x15 주소의 값을 가져옴
// 결과: 'I'
```

### 메모리 계산 과정

```
s[3] 접근:
┌─────┬─────┬─────┬─────┬─────┐
│ [0] │ [1] │ [2] │ [3] │ ... │
│  B  │  A  │  S  │  I  │ ... │
└─────┴─────┴─────┴─────┴─────┘
                    ↑
                    s[3]

*(p + 3) 접근:
p (0x12)
  │
  ├─ +1 → 0x13
  ├─ +2 → 0x14
  └─ +3 → 0x15 ← 여기 값(*) 가져오기
```

---

## 4. 배열과 포인터 표기법 비교

### 동등한 표현들

```c
char s[8] = "BASIC-C";
char *p = s;
```

| 배열 표기 | 포인터 표기 | 의미 | 결과 |
|-----------|-------------|------|------|
| `s[0]` | `*p` 또는 `*(p+0)` | 첫 번째 요소 | 'B' |
| `s[1]` | `*(p+1)` | 두 번째 요소 | 'A' |
| `s[2]` | `*(p+2)` | 세 번째 요소 | 'S' |
| `s[3]` | `*(p+3)` | 네 번째 요소 | 'I' |

### 종합 예제

```c
#include <stdio.h>

int main() {
    char s[8] = "BASIC-C";
    char *p = s;
    
    // 같은 결과를 내는 다양한 표현
    printf("s[3] = %c\n", s[3]);         // I
    printf("*(s+3) = %c\n", *(s+3));     // I
    printf("p[3] = %c\n", p[3]);         // I
    printf("*(p+3) = %c\n", *(p+3));     // I
    
    return 0;
}
```

### 코드 해설

```c
printf("s[3] = %c\n", s[3]);
// 배열 표기법
// s의 3번 인덱스 요소
// 결과: 'I'

printf("*(s+3) = %c\n", *(s+3));
// 포인터 산술 연산
// s + 3: 시작 주소에서 3칸 이동한 주소
// *(s+3): 그 주소의 값
// 결과: 'I'

printf("p[3] = %c\n", p[3]);
// 포인터를 배열처럼 사용
// p는 s를 가리키므로 p[3]은 s[3]과 같음
// 결과: 'I'

printf("*(p+3) = %c\n", *(p+3));
// 포인터 산술 연산
// p + 3: 시작 주소에서 3칸 이동
// *(p+3): 그 주소의 값
// 결과: 'I'
```

---

## 5. 주소 출력 vs 값 출력

### 중요한 차이점

```c
char s[8] = "BASIC-C";
char *p = s;

// 주소 출력
printf("%p\n", s+3);      // 주소 출력
printf("%p\n", p+3);      // 주소 출력

// 값 출력
printf("%c\n", s[3]);     // 값 출력
printf("%c\n", *(p+3));   // 값 출력
```

### 상세 예제

```c
#include <stdio.h>

int main() {
    char s[8] = "BASIC-C";
    char *p = s;
    
    printf("--- 주소 출력 ---\n");
    printf("&s[3] = %p\n", &s[3]);    // 주소
    printf("s+3 = %p\n", s+3);        // 주소
    printf("p+3 = %p\n", p+3);        // 주소
    
    printf("\n--- 값 출력 ---\n");
    printf("s[3] = %c\n", s[3]);      // 값
    printf("*(s+3) = %c\n", *(s+3));  // 값
    printf("*(p+3) = %c\n", *(p+3));  // 값
    
    return 0;
}
```

### 실행 결과

```
--- 주소 출력 ---
&s[3] = 0x7ffd5467e8c3
s+3 = 0x7ffd5467e8c3
p+3 = 0x7ffd5467e8c3

--- 값 출력 ---
s[3] = I
*(s+3) = I
*(p+3) = I
```

---

## 6. 문자열 출력의 특별한 경우

### 주소를 출력하면 문자열이 출력됩니다

```c
char s[8] = "BASIC-C";
char *p = s;

printf("%s\n", p+3);     // "IC-C" 출력
printf("%s\n", &s[3]);   // "IC-C" 출력
```

### 왜 그럴까요?

```c
printf("%s\n", p+3);
// %s: 문자열 출력 형식 지정자
// p+3: 3번 인덱스의 주소 (0x15)
// 동작: 0x15부터 시작해서 \0을 만날 때까지 출력
```

### 메모리 관점

```
주소:    0x12  0x13  0x14  0x15  0x16  0x17  0x18  0x19
       ┌─────┬─────┬─────┬─────┬─────┬─────┬─────┬─────┐
값:    │  B  │  A  │  S  │  I  │  C  │  -  │  C  │ \0  │
       └─────┴─────┴─────┴─────┴─────┴─────┴─────┴─────┘
                          ↑
                          │
                       p+3 (0x15)
                          │
        %s 출력 시작 ─────┘
        0x15부터 \0까지: "IC-C"
```

### 상세 예제

```c
#include <stdio.h>

int main() {
    char s[8] = "BASIC-C";
    char *p = s;
    
    printf("--- 문자 하나만 출력 ---\n");
    printf("s[3] = %c\n", s[3]);        // I
    printf("*(p+3) = %c\n", *(p+3));    // I
    
    printf("\n--- 문자열 출력 ---\n");
    printf("s+3 = %s\n", s+3);          // IC-C
    printf("p+3 = %s\n", p+3);          // IC-C
    printf("&s[3] = %s\n", &s[3]);      // IC-C
    
    return 0;
}
```

### 코드 해설

```c
// 문자 하나 출력 (%c 사용)
printf("s[3] = %c\n", s[3]);
// %c: 문자 하나만 출력
// s[3]: 'I'
// 결과: I

// 문자열 출력 (%s 사용)
printf("s+3 = %s\n", s+3);
// %s: 문자열 출력 (널 문자까지)
// s+3: 3번 인덱스부터 시작
// 결과: IC-C (I, C, -, C, \0)

printf("p+3 = %s\n", p+3);
// %s: 문자열 출력
// p+3: 3번 인덱스 주소
// 결과: IC-C

printf("&s[3] = %s\n", &s[3]);
// %s: 문자열 출력
// &s[3]: 3번 인덱스의 주소
// 결과: IC-C
```

:::tip %c vs %s
```c
%c: 문자 하나만 출력 → 'I'
%s: 널 문자까지 모두 출력 → "IC-C"
```
:::

---

## 7. 배열과 포인터의 차이점

### 비슷하지만 다릅니다

```c
char s[8] = "BASIC-C";  // 배열
char *p = s;            // 포인터
```

### 주요 차이점

| 특성 | 배열 | 포인터 |
|------|------|--------|
| 메모리 | 실제 데이터 저장 | 주소만 저장 |
| 크기 | 고정 (선언 시 결정) | 포인터 크기 (8바이트) |
| 재할당 | 불가능 | 가능 |
| sizeof | 전체 배열 크기 | 포인터 크기 |

### 예제로 확인

```c
#include <stdio.h>

int main() {
    char s[8] = "BASIC-C";
    char *p = s;
    
    printf("sizeof(s) = %zu\n", sizeof(s));   // 8
    printf("sizeof(p) = %zu\n", sizeof(p));   // 8 (64비트 시스템)
    
    // s = "OTHER";  // ❌ 오류! 배열 이름은 상수
    p = "OTHER";     // ✅ 가능! 포인터는 재할당 가능
    
    return 0;
}
```

### 코드 해설

```c
printf("sizeof(s) = %zu\n", sizeof(s));
// sizeof(s): 배열 전체 크기
// s는 char[8]이므로 8바이트
// 결과: 8

printf("sizeof(p) = %zu\n", sizeof(p));
// sizeof(p): 포인터 변수의 크기
// 64비트 시스템에서 포인터는 8바이트
// 결과: 8 (포인터 크기)

// 배열은 재할당 불가
s = "OTHER";  // ❌ 컴파일 오류!
// 배열 이름은 상수 포인터
// 다른 곳을 가리킬 수 없음

// 포인터는 재할당 가능
p = "OTHER";  // ✅ 정상 동작
// p가 새로운 문자열을 가리킴
```

---

## 학습 정리

### 핵심 개념 체크리스트

✅ **배열 이름은 첫 번째 요소의 주소**를 나타냅니다  
✅ **배열과 포인터는 호환**이 가능합니다  
✅ **arr[i] == *(arr+i)** 두 표현은 동일합니다  
✅ **%s로 주소를 출력**하면 그 위치부터 문자열이 출력됩니다  
✅ **배열은 재할당 불가, 포인터는 재할당 가능**합니다  

### 표기법 정리

```c
char s[8] = "BASIC-C";
char *p = s;

// 모두 같은 의미
s[3]      // 배열 표기
*(s+3)    // 포인터 표기
p[3]      // 포인터를 배열처럼
*(p+3)    // 포인터 산술

// 문자 vs 문자열
%c, s[3]    → 'I' (문자 하나)
%s, s+3     → "IC-C" (문자열)
```

### 실전 팁

```c
// 배열 요소 접근
for (int i = 0; i < 8; i++) {
    printf("%c", s[i]);      // 방법 1: 배열 표기
    printf("%c", *(s+i));    // 방법 2: 포인터 표기
    printf("%c", *(p+i));    // 방법 3: 포인터 변수
}
```

---

## 다음 단계

이제 실제 코드로 **포인터를 활용하는 다양한 예제**를 살펴보겠습니다! 
두 값을 교환하는 프로그램 등 실용적인 예제를 만나보세요! 🚀