---
title: "포인터의 개념과 선언"
description: "C언어의 핵심인 포인터의 개념을 이해하고 선언하는 방법을 배웁니다."
slug: "pointer-concept"
sidebar_position: 2
---

# 포인터의 개념과 선언

## 학습 목표
- 포인터가 무엇인지 이해합니다
- 포인터 변수를 선언하는 방법을 배웁니다
- 포인터의 초기화 방법을 학습합니다

---

## 1. 포인터란 무엇인가요?

### 포인터의 정의

**포인터(Pointer)**는 다른 변수의 **메모리 주소를 저장**하는 특별한 변수입니다.

```
일반 변수:      값을 저장
포인터 변수:    주소를 저장 (값이 있는 위치를 가리킴)
```

### 간단한 예시

```c
int n = 100;        // 일반 변수: 100이라는 값을 저장
int *ptr = &n;      // 포인터 변수: n의 주소를 저장
```

```
메모리 상태:

[변수 n]
주소: 0x12
값:   100
     ┌─────────┐
     │   100   │  ← n에 저장된 실제 값
     └─────────┘

[포인터 ptr]
주소: 0x52
값:   0x12
     ┌─────────┐
     │  0x12   │  ← n의 주소를 저장!
     └─────────┘
          │
          └──────→ 0x12 주소를 가리킴 (pointing)
```

---

## 2. 포인터는 왜 필요한가요?

### 실생활 비유: 바로가기 아이콘 🖱️

포인터는 **컴퓨터의 바로가기 아이콘**과 비슷합니다!

```
[바탕화면의 Word 바로가기]
┌──────────────┐
│   📄 Word    │  ← 바로가기 아이콘
└──────────────┘
       │
       │ (실제 프로그램 위치를 가리킴)
       ↓
C:\Program Files\Microsoft\Word\winword.exe
```

### 포인터와 바로가기의 공통점

1. **실제 프로그램은 다른 곳에** 있습니다
2. **바로가기는 그 위치를 가리키고** 있습니다
3. 바로가기를 클릭하면 **실제 프로그램이 실행**됩니다

```c
int word = 100;        // 실제 프로그램 (Word)
int *shortcut = &word; // 바로가기 아이콘
```

:::tip 포인터의 장점
- **메모리를 효율적으로 관리**할 수 있습니다
- **큰 데이터를 복사하지 않고** 주소만 전달할 수 있습니다
- **배열과 함께 사용**하면 데이터를 쉽게 다룰 수 있습니다
:::

---

## 3. 포인터 변수 선언하기

### 기본 문법

```c
자료형 *포인터변수명;
```

### 예제 코드

```c
int *p;      // 정수형 포인터 변수 선언
double *d;   // 실수형 포인터 변수 선언
char *c;     // 문자형 포인터 변수 선언
```

### 코드 해설 (한 줄씩)

```c
int *p;
// int: 포인터가 가리킬 변수의 자료형
// *: 포인터 변수임을 나타내는 기호
// p: 포인터 변수의 이름

double *d;
// double: 실수형 변수의 주소를 저장
// *d: d는 포인터 변수

char *c;
// char: 문자형 변수의 주소를 저장
// *c: c는 포인터 변수
```

### 별표(*) 기호의 의미

:::warning 별표의 위치와 의미
```c
// 선언할 때의 별표
int *ptr;    // ptr은 포인터 변수입니다

// 사용할 때의 별표
*ptr = 10;   // ptr이 가리키는 곳에 10을 저장
```

- **선언 시**: 포인터 변수임을 표시
- **사용 시**: 포인터가 가리키는 값을 의미
:::

---

## 4. 포인터 초기화하기

### NULL 포인터

변수를 만들면 항상 **초기화**해야 합니다. 포인터도 마찬가지입니다!

```c
int *p = NULL;  // NULL로 초기화
```

### 코드 해설

```c
int *p = NULL;
// int *p: 정수형 포인터 변수 p를 선언
// = NULL: 아직 아무것도 가리키지 않는 상태로 초기화
```

### 왜 NULL로 초기화하나요?

```c
// 초기화하지 않은 포인터 (위험! ❌)
int *p;
// p에는 쓰레기 값이 들어있음
// 어디를 가리키는지 알 수 없음

// NULL로 초기화한 포인터 (안전! ✅)
int *p = NULL;
// p는 아무것도 가리키지 않음을 명확히 표시
// 사용 전에 안전하게 확인 가능
```

:::danger 초기화하지 않으면?
```c
int *p;           // 초기화 안 함
*p = 100;         // ❌ 위험! 어디에 100을 저장할지 모름!

int *p = NULL;    // NULL로 초기화
if (p != NULL) {  // ✅ 안전! 사용 전 확인 가능
    *p = 100;
}
```
:::

---

## 5. 포인터에 주소 저장하기

### & 연산자 (주소 연산자)

변수의 주소를 얻으려면 **& 연산자**를 사용합니다.

```c
int a = 10;      // 일반 변수
int *p = &a;     // a의 주소를 p에 저장
```

### 코드 해설

```c
int a = 10;
// int a: 정수형 변수 a를 선언
// = 10: a에 10을 저장

int *p = &a;
// int *p: 정수형 포인터 변수 p를 선언
// = &a: a의 주소를 p에 저장
// &: 주소 연산자 (a의 주소를 가져옴)
```

### 메모리 상태 확인

```
실행 전:
    a: [비어있음]
    p: [비어있음]

int a = 10; 실행 후:
    a: 주소 0x12에 값 100 저장
    ┌──────┐
    │  10  │  0x12번지
    └──────┘

int *p = &a; 실행 후:
    p: 주소 0x52에 0x12 저장
    ┌──────┐
    │ 0x12 │  0x52번지
    └──────┘
       │
       └──→ a를 가리킴
```

---

## 6. 포인터 사용 예제

### 기본 예제

```c
#include <stdio.h>

int main() {
    int n = 100;           // 정수 변수 선언
    int *ptr = &n;         // 포인터 선언 및 초기화
    
    printf("n의 값: %d\n", n);           // 100
    printf("n의 주소: %p\n", &n);        // 0x12 (예시)
    printf("ptr의 값: %p\n", ptr);       // 0x12 (같은 주소)
    printf("ptr이 가리키는 값: %d\n", *ptr);  // 100
    
    return 0;
}
```

### 코드 해석 (라인별)

```c
int n = 100;
// 라인 1: 정수형 변수 n을 선언하고 100으로 초기화
// 메모리: n이 어딘가(예: 0x12)에 생성되고 100이 저장됨

int *ptr = &n;
// 라인 2: 정수형 포인터 ptr을 선언
// &n: n의 주소를 가져옴 (예: 0x12)
// ptr에 0x12가 저장됨

printf("n의 값: %d\n", n);
// 라인 3: n에 저장된 값을 출력
// 결과: 100

printf("n의 주소: %p\n", &n);
// 라인 4: n의 메모리 주소를 출력
// %p: 주소를 출력하는 형식 지정자
// 결과: 0x12 (실제로는 더 긴 주소)

printf("ptr의 값: %p\n", ptr);
// 라인 5: ptr에 저장된 주소를 출력
// ptr은 n의 주소를 저장하고 있음
// 결과: 0x12 (n의 주소와 동일)

printf("ptr이 가리키는 값: %d\n", *ptr);
// 라인 6: ptr이 가리키는 위치의 값을 출력
// *ptr: ptr이 가리키는 곳(0x12)의 값
// 결과: 100
```

### 실행 결과

```
n의 값: 100
n의 주소: 0x7ffd5467e8c4
ptr의 값: 0x7ffd5467e8c4
ptr이 가리키는 값: 100
```

:::note 주소는 매번 달라요!
실제 주소 값(0x7ffd5467e8c4)은 실행할 때마다 다를 수 있습니다.
중요한 것은 **n의 주소와 ptr의 값이 같다**는 점입니다!
:::

---

## 7. 포인터 자료형의 중요성

### 자료형을 맞춰야 합니다

```c
int a = 10;
int *p1 = &a;      // ✅ 올바름: int 변수 → int 포인터

double b = 3.14;
int *p2 = &b;      // ❌ 잘못됨: double 변수 → int 포인터
```

### 왜 자료형을 맞춰야 하나요?

```c
int *p;      // int 포인터: 4바이트 읽음
double *d;   // double 포인터: 8바이트 읽음
```

포인터는 가리키는 곳에서 **몇 바이트를 읽어야 하는지** 알아야 합니다!

```
int 포인터가 가리킬 때:
┌────┬────┬────┬────┐
│  4바이트를 읽음   │
└────┴────┴────┴────┘

double 포인터가 가리킬 때:
┌────┬────┬────┬────┬────┬────┬────┬────┐
│         8바이트를 읽음            │
└────┴────┴────┴────┴────┴────┴────┴────┘
```

---

## 학습 정리

### 핵심 개념 체크리스트

✅ **포인터는 주소를 저장**하는 변수입니다  
✅ **선언할 때 별표(*)를 붙여** 포인터임을 표시합니다  
✅ **NULL로 초기화**하여 안전하게 사용합니다  
✅ **& 연산자**로 변수의 주소를 얻습니다  
✅ **포인터의 자료형**과 **가리키는 변수의 자료형**을 일치시킵니다  

### 용어 정리

| 용어 | 기호 | 설명 |
|------|------|------|
| 포인터 변수 | `int *p` | 주소를 저장하는 변수 |
| 주소 연산자 | `&` | 변수의 주소를 가져옴 |
| NULL 포인터 | `NULL` | 아무것도 가리키지 않는 상태 |

### 기본 패턴

```c
// 1단계: 일반 변수 선언
int n = 100;

// 2단계: 포인터 선언 및 초기화
int *ptr = NULL;

// 3단계: 주소 저장
ptr = &n;

// 이렇게 한 번에 할 수도 있습니다
int *ptr = &n;  // 2단계와 3단계를 합침
```

---

## 다음 단계

이제 포인터로 **실제 값을 어떻게 변경하는지** 배워보겠습니다! 
포인터 연산자에 대해 알아볼까요? 🚀