---
title: 'Join 연산의 구현 방법'
description: '두 테이블을 연결하는 3가지 조인 방식과 각각의 특징을 알아봅시다'
slug: 'database/join-implementation-methods'
sidebar_label: 'Join 연산의 구현 방법'
sidebar_position: 2
date: '2025-02-01'
---

# Join 연산의 구현 방법

## 📌 Join의 역할

두 개 이상의 테이블에서 필요한 데이터를 함께 가져오기 위해 테이블들을 연결합니다.

```
테이블 R: 학생 정보          테이블 S: 성적 정보
┌───────┬────┐           ┌───────┬────────┐
│학번   │이름 │           │학번   │ 점수   │
├───────┼────┤           ├───────┼────────┤
│ 1001  │김철수│          │ 1001  │ 85점   │
│ 1002  │이영희│          │ 1002  │ 92점   │
│ 1003  │박민수│          │ 1003  │ 78점   │
└───────┴────┘           └───────┴────────┘

Join: 같은 학번끼리 연결하여 학생별 성적 정보 획득
```

---

## 🔗 1) 정렬-병합 조인 (Sort-Merge Join)

두 테이블을 모두 정렬한 후 동시에 비교하며 병합합니다.

### 과정

```
Step 1: R 테이블 정렬
[1001, 1002, 1003, 1004] → 이미 정렬됨

Step 2: S 테이블 정렬
[1003, 1001, 1002, 1004] → [1001, 1002, 1003, 1004]로 정렬

Step 3: 양쪽을 동시에 비교하며 병합
R: [1001, 1002, 1003, 1004]
    ↓    ↓    ↓    ↓
S: [1001, 1002, 1003, 1004]

결과:
┌───────┬────┬────────┐
│학번   │이름 │ 점수   │
├───────┼────┼────────┤
│ 1001  │김철수│ 85점   │
│ 1002  │이영희│ 92점   │
│ 1003  │박민수│ 78점   │
│ 1004  │...  │ ...    │
└───────┴────┴────────┘
```

### 특징

```
장점:
+ 전체 데이터를 모두 처리하는 경우 효율적
+ 메모리 사용 최소화
+ 대용량 데이터 처리에 좋음

단점:
- 반드시 정렬 필요 (비용 발생)
- 부분 조인에는 비효율적
```

### 언제 사용할까?

```
✓ 두 테이블 모두 완전히 조인하는 경우
✓ 이미 정렬된 데이터인 경우
✓ 전체 행을 처리해야 하는 경우
✓ 배치 작업

✗ 특정 행만 필요한 경우
✗ 조인 조건이 복잡한 경우
```

---

## 🔗 2) 중첩 루프 조인 (Nested Loop Join)

외부 테이블의 각 행에 대해 내부 테이블 전체를 검색하는 방식입니다.

### 과정

```
외부 테이블 R (Driving Table)
─────────────────
1001 ──┐
       │
1002 ──┤  각 행마다 S 전체를 검색
       │
1003 ──┘

내부 테이블 S (Inner Table)
1001, 1002, 1003, 1004 (매번 전체 검색)

동작:
FOR 각 R의 행:
  FOR 각 S의 행:
    IF 학번 같으면:
      결과에 추가
```

### 코드 표현

```javascript
// 의사코드
for (let r of R) {
  // R의 각 행마다
  for (let s of S) {
    // S를 처음부터 전체 검색
    if (r.학번 == s.학번) {
      // 매칭되면 결과 저장
      result.add(r, s);
    }
  }
}
```

### 성능 분석

```
R의 행: 100개
S의 행: 1000개

총 비교 횟수: 100 × 1000 = 100,000회

예제 성능:
- 정렬-병합: 1001 + 1002 + 1003 정렬 비용
- 중첩 루프: 100 × 1000 = 100,000 비교

→ 데이터 크기와 인덱스 여부에 따라 우열이 결정됨
```

### 최적화: 인덱스 사용

```
내부 테이블 S에 인덱스가 있으면?

FOR 각 R의 행:
  S에서 인덱스로 직접 검색
    (전체 검색 대신)

성능 개선:
- 비교 횟수: 100 × 1000 → 100 × log(1000)
- 약 100배 빨라짐!
```

### 특징

```
장점:
+ 정렬 비용 없음
+ 메모리 효율적
+ 작은 외부 테이블에 효과적
+ 인덱스로 크게 최적화 가능

단점:
- 많은 비교 필요
- 외부 테이블이 크면 비효율적
```

### 언제 사용할까?

```
✓ 외부 테이블이 작은 경우
✓ 내부 테이블에 인덱스가 있는 경우
✓ 조인 조건이 인덱스 컬럼인 경우
✓ 특정 행만 필요한 경우

✗ 두 테이블 모두 큰 경우
✗ 인덱스가 없는 경우
```

---

## 🔗 3) 해시 조인 (Hash Join)

해시 함수를 사용하여 같은 해시값을 가진 행들끼리 비교합니다.

### 과정

```
Step 1: 작은 테이블(S)을 메모리에 해시 테이블 생성
학번  → 해시값
1001  → 001
1002  → 002
1003  → 003

Step 2: 큰 테이블(R)을 읽으며 해시값 계산
R의 1001: h(1001) = 001 → 해시 테이블에서 1001 찾음 ✓

Step 3: 매칭되는 행 결합
```

### 코드 표현

```javascript
// 해시 테이블 생성 (S를 메모리에)
let hashTable = {};
for (let s of S) {
  let key = h(s.학번);  // 해시 함수 적용
  if (!hashTable[key]) {
    hashTable[key] = [];
  }
  hashTable[key].push(s);
}

// R을 읽으며 해시 테이블 조회
for (let r of R) {
  let key = h(r.학번);
  let matches = hashTable[key];
  
  for (let s of matches) {
    if (r.학번 == s.학번) {
      result.add(r, s);
    }
  }
}
```

### 성능 분석

```
R: 1,000,000행 (큰 테이블)
S: 10,000행 (작은 테이블)

과정:
1. S를 메모리에 로드: 10,000
2. R을 읽으며 해시 조회: 1,000,000 × O(1)
3. 총 비용: 약 1,010,000

중첩 루프 대비:
- 중첩 루프: 1,000,000 × 10,000 = 10,000,000,000회
- 해시 조인: 약 1,010,000회

→ 약 10,000배 빠름!
```

### 특징

```
장점:
+ 매우 빠른 조인 (O(n))
+ 정렬 비용 없음
+ 대용량 데이터에 최적
+ 인덱스 필요 없음

단점:
- 메모리 충분해야 함
- 범위 조인에 부적합
- 해시 충돌 처리 비용
```

### 언제 사용할까?

```
✓ 한 테이블이 작아서 메모리에 로드 가능
✓ 동등 조인 (=) 조건
✓ 매우 큰 테이블 간 조인
✓ 인덱스가 없는 경우

✗ 범위 조인 (>, <)
✗ 메모리가 부족한 경우
✗ 아우터 조인 (일부)
```

---

## 📊 3가지 방법 비교

```
┌─────────────────┬────────────┬────────────┬─────────────┐
│ 조인 방식       │ 정렬 필요  │ 메모리     │ 최적 상황   │
├─────────────────┼────────────┼────────────┼─────────────┤
│정렬-병합 조인   │ 필수       │ 적음       │전체 조인    │
│중첩 루프 조인   │ 불필요    │ 적음       │작은 외부    │
│해시 조인        │ 불필요    │ 많음       │큰 테이블    │
└─────────────────┴────────────┴────────────┴─────────────┘
```

---

## 💡 선택 가이드

```
상황: 두 테이블 조인 필요

1. 테이블 크기 확인
   ├─ 둘 다 큼 → 해시 조인
   ├─ 하나 작음 → 중첩 루프 조인
   └─ 모두 중간 → 정렬-병합 조인

2. 인덱스 유무 확인
   ├─ 있음 → 중첩 루프 조인 고려
   └─ 없음 → 해시 조인 고려

3. 메모리 상황 확인
   ├─ 충분 → 해시 조인
   └─ 부족 → 정렬-병합 조인
```

---

**작성일**: 2025-02-01  
**난이도**: ⭐⭐⭐