---
title: DBMS 구성요소
description: DBMS의 내부 구조와 각 모듈의 역할, 데이터 처리 흐름을 학습합니다.
slug: dbms-components
sidebar_position: 6
---

# DBMS 구성요소

DBMS가 어떻게 구성되어 있고, 각 모듈이 어떤 역할을 하는지 살펴보겠습니다.

---

## 1. DBMS 사용자 유형

### 3가지 사용자 유형

```
1. 일반 사용자 (End User)
2. 응용 프로그래머 (Application Programmer)
3. 데이터베이스 관리자 (DBA - Database Administrator)
```

---

### 1.1 일반 사용자 (End User)

#### 특징
```
- 데이터베이스를 직접 사용
- 프로그래밍 지식 불필요
- 질의어를 이용한 접근
```

#### 사용 방법
**질의어 (Query Language) 사용**

#### 예시
```sql
-- 은행 ATM 사용자
SELECT 잔액 FROM 계좌 WHERE 계좌번호 = '1234-5678';

-- 학생 포털 사용자
SELECT 성적 FROM 수강 WHERE 학번 = '2024001';

-- 쇼핑몰 고객
SELECT 상품명, 가격 FROM 상품 WHERE 카테고리 = '전자제품';
```

#### 실제 사용 사례
```
✅ ATM에서 잔액 조회
✅ 학생 포털에서 성적 확인
✅ 온라인 쇼핑몰에서 상품 검색
✅ 항공권 예약 시스템
```

---

### 1.2 응용 프로그래머 (Application Programmer)

#### 특징
```
- 데이터베이스를 활용한 프로그램 개발
- 호스트 언어 + SQL 결합
- DML(데이터 조작어) 사용
```

#### 사용 방법
**데이터 조작어 (DML - Data Manipulation Language)**

#### 호스트 언어 + SQL 결합

```java
// Java + SQL 예시
public class StudentDAO {
    public Student getStudent(String id) {
        String sql = "SELECT * FROM 학생 WHERE 학번 = ?";
        // SQL을 Java 코드에 삽입
        PreparedStatement pstmt = conn.prepareStatement(sql);
        pstmt.setString(1, id);
        ResultSet rs = pstmt.executeQuery();
        // 결과 처리
        return student;
    }
}
```

```python
# Python + SQL 예시
def get_student(student_id):
    sql = "SELECT * FROM 학생 WHERE 학번 = %s"
    cursor.execute(sql, (student_id,))
    result = cursor.fetchone()
    return result
```

#### 개발 예시
```
웹 애플리케이션 개발:
- 회원가입 기능
- 로그인 기능
- 게시판 CRUD
- 상품 관리 시스템
```

---

### 1.3 데이터베이스 관리자 (DBA)

#### 특징
```
- 데이터베이스 전체 관리
- 스키마 설계 및 관리
- DDL(데이터 정의어) 사용
```

#### 사용 방법
**데이터 정의어 (DDL - Data Definition Language)**

#### 주요 업무

**1. 스키마 설계**
```sql
-- 테이블 생성
CREATE TABLE 학생 (
    학번 VARCHAR(10) PRIMARY KEY,
    이름 VARCHAR(50) NOT NULL,
    학과 VARCHAR(50),
    학년 INT CHECK (학년 BETWEEN 1 AND 4)
);
```

**2. 인덱스 관리**
```sql
-- 성능 향상을 위한 인덱스 생성
CREATE INDEX idx_학과 ON 학생(학과);
```

**3. 권한 관리**
```sql
-- 사용자 생성 및 권한 부여
CREATE USER '사용자A' IDENTIFIED BY 'password';
GRANT SELECT, INSERT ON 학생 TO '사용자A';
```

**4. 백업 및 복구**
```bash
# 백업
mysqldump -u root -p database_name > backup.sql

# 복구
mysql -u root -p database_name < backup.sql
```

**5. 성능 모니터링**
```sql
-- 느린 쿼리 확인
SHOW PROCESSLIST;

-- 테이블 최적화
OPTIMIZE TABLE 학생;
```

---

### 사용자 유형별 비교

| 구분 | 일반 사용자 | 응용 프로그래머 | DBA |
|------|------------|----------------|-----|
| **사용 언어** | 질의어 | DML | DDL |
| **주요 작업** | 데이터 조회 | 프로그램 개발 | 스키마 설계 |
| **기술 수준** | 낮음 | 중간 | 높음 |
| **권한** | 제한적 | 중간 | 최고 |
| **예시** | ATM 사용자 | 웹 개발자 | DB 관리자 |

---

## 2. DBMS 내부 구성요소

### 전체 구조 개요

```
┌─────────────────────────────────────┐
│         사용자/프로그래머           │
└─────────────────┬───────────────────┘
                  ↓
┌─────────────────────────────────────┐
│     DDL 컴파일러 (정의어 처리)     │
├─────────────────────────────────────┤
│     DML 컴파일러 (조작어 처리)     │
├─────────────────────────────────────┤
│      질의 처리기 (쿼리 실행)        │
├─────────────────────────────────────┤
│  런타임 데이터베이스 처리기 (실행)  │
├─────────────────────────────────────┤
│     트랜잭션 관리자 (제어)         │
├─────────────────────────────────────┤
│   저장 데이터 관리자 (저장)        │
└─────────────────┬───────────────────┘
                  ↓
┌─────────────────────────────────────┐
│      데이터 사전 (메타데이터)      │
│         데이터베이스 (실제 데이터)  │
└─────────────────────────────────────┘
```

---

## 3. 각 모듈의 역할

### 3.1 DDL 컴파일러 (DDL Compiler)

#### 역할
스키마 정의를 컴파일하여 메타데이터를 생성합니다.

#### 처리 과정

```
사용자(DBA)
    ↓
DDL 명령어 입력
    ↓
DDL 컴파일러
    ↓
메타데이터 생성
    ↓
데이터 사전(카탈로그)에 저장
```

#### 예시

```sql
-- DBA가 입력
CREATE TABLE 학생 (
    학번 VARCHAR(10) PRIMARY KEY,
    이름 VARCHAR(50)
);

-- DDL 컴파일러 처리
↓

-- 데이터 사전에 저장되는 메타데이터
테이블명: 학생
컬럼1: 학번, VARCHAR(10), PRIMARY KEY
컬럼2: 이름, VARCHAR(50)
```

#### 특징
```
✅ 구조 정의만 처리
✅ 실제 데이터는 건드리지 않음
✅ 메타데이터는 데이터 사전에 저장
✅ 실행 단계를 거치지 않고 바로 저장
```

---

### 3.2 DML 예비 컴파일러 (DML Precompiler)

#### 역할
호스트 언어와 DML을 분리합니다.

#### 처리 과정

```
응용 프로그램
(Java + SQL)
    ↓
DML 예비 컴파일러
    ↓
Java 코드 ──→ Java 컴파일러
SQL 코드  ──→ DML 컴파일러
```

#### 예시

**입력 (혼합 코드):**
```java
public void insertStudent() {
    // Java 코드
    String id = "2024001";
    String name = "홍길동";
    
    // SQL 코드 (DML)
    String sql = "INSERT INTO 학생 VALUES (?, ?)";
    pstmt = conn.prepareStatement(sql);
    
    // Java 코드
    pstmt.setString(1, id);
    pstmt.setString(2, name);
}
```

**DML 예비 컴파일러 처리 후:**
```
[Java 부분]
String id = "2024001";
String name = "홍길동";
pstmt.setString(1, id);
pstmt.setString(2, name);

[SQL 부분]
INSERT INTO 학생 VALUES (?, ?)
```

#### 특징
```
✅ 호스트 언어와 SQL 분리
✅ 각각 적절한 컴파일러로 전달
✅ 언어 간 인터페이스 역할
```

---

### 3.3 DML 컴파일러 (DML Compiler)

#### 역할
DML 명령어를 파싱하고 컴파일합니다.

#### 처리 과정

```
SQL 코드
    ↓
DML 컴파일러
    ↓
파싱 (구문 분석)
    ↓
최적화
    ↓
실행 계획 생성
    ↓
런타임 처리기로 전달
```

#### 예시

```sql
-- 입력
SELECT 이름, 학과 
FROM 학생 
WHERE 학년 = 3;

-- DML 컴파일러 처리
1. 파싱: SELECT, FROM, WHERE 구문 분석
2. 검증: 테이블 존재 확인, 컬럼 존재 확인
3. 최적화: 인덱스 사용 여부 결정
4. 실행 계획: 어떻게 데이터를 가져올지 결정
```

#### 특징
```
✅ SQL 문법 검사
✅ 쿼리 최적화
✅ 실행 계획 생성
✅ 런타임 처리기로 전달
```

---

### 3.4 질의 처리기 (Query Processor)

#### 역할
사용자의 질의문을 받아 처리합니다.

#### 처리 과정

```
일반 사용자
    ↓
질의문 입력 (SQL)
    ↓
질의 처리기
    ↓
파싱 (Parsing)
    ↓
분석 (Analysis)
    ↓
최적화 (Optimization)
    ↓
런타임 처리기로 전달
```

#### 파싱 예시

```sql
-- 입력 쿼리
SELECT * FROM 학생 WHERE 학년 = 3;

-- 파싱 (토큰 분리)
Token 1: SELECT
Token 2: *
Token 3: FROM
Token 4: 학생
Token 5: WHERE
Token 6: 학년
Token 7: =
Token 8: 3
```

#### 특징
```
✅ 고급 질의문 처리
✅ 구문 분석 (Parsing)
✅ 의미 분석 (Semantic Analysis)
✅ 쿼리 최적화
```

---

### 3.5 런타임 데이터베이스 처리기 (Runtime Database Processor)

#### 역할
실제로 데이터베이스에 접근하여 명령을 실행합니다.

#### 처리 과정

```
컴파일된 명령어
    ↓
런타임 처리기
    ↓
실행 준비
    ↓
트랜잭션 관리자에 전달
    ↓
실제 데이터 처리
    ↓
결과 반환
```

#### 주요 기능

**1. 데이터 읽기**
```
SELECT 쿼리 실행
    ↓
테이블 스캔 또는 인덱스 스캔
    ↓
조건에 맞는 레코드 검색
    ↓
결과 반환
```

**2. 데이터 쓰기**
```
INSERT/UPDATE/DELETE 실행
    ↓
트랜잭션 시작
    ↓
데이터 수정
    ↓
커밋 또는 롤백
```

#### 특징
```
✅ 실제 실행 담당
✅ 데이터베이스 접근
✅ 결과 생성
✅ 성능에 직접적 영향
```

---

### 3.6 트랜잭션 관리자 (Transaction Manager)

#### 역할
데이터의 무결성, 보안, 병행 제어, 회복 등을 관리합니다.

#### 주요 기능

**1. 무결성 검사 (Integrity Check)**
```sql
-- 제약조건 검증
INSERT INTO 학생 VALUES ('2024001', '홍길동', 5);
                                            ↑
트랜잭션 관리자 검사:
학년이 5? → CHECK 제약 위반! → 거부
```

**2. 권한 검사 (Authorization Check)**
```sql
-- 사용자 권한 확인
사용자A: DELETE FROM 학생;
         ↓
트랜잭션 관리자 검사:
사용자A에게 DELETE 권한 있는가? 
권한 없음 → 거부
```

**3. 병행 제어 (Concurrency Control)**
```
사용자A: UPDATE 계좌 SET 잔액 = 잔액 - 10000;
사용자B: SELECT 잔액 FROM 계좌;

트랜잭션 관리자:
- 잠금(Lock) 설정
- 동시 실행 조율
- 충돌 방지
```

**4. 회복 (Recovery)**
```
트랜잭션 실행 중 오류 발생
    ↓
트랜잭션 관리자:
- 로그 확인
- ROLLBACK 수행
- 이전 상태로 복원
```

#### 처리 흐름

```
런타임 처리기
    ↓
트랜잭션 관리자
    ↓
1. 무결성 검사 ✓
2. 권한 검사 ✓
3. 병행 제어 ✓
    ↓
저장 데이터 관리자로 전달
    ↓
(실행 완료 후)
4. 회복 처리 (필요시)
```

#### 특징
```
✅ 데이터 안전성 보장
✅ 동시성 제어
✅ 장애 복구
✅ 보안 관리
```

---

### 3.7 저장 데이터 관리자 (Stored Data Manager)

#### 역할
실제 디스크에 데이터를 저장하고 읽어옵니다.

#### 처리 과정

```
트랜잭션 관리자로부터 명령 수신
    ↓
저장 데이터 관리자
    ↓
운영체제에 I/O 요청
    ↓
디스크 읽기/쓰기
    ↓
결과 반환
```

#### 주요 기능

**1. 데이터 저장**
```
메모리의 데이터
    ↓
저장 데이터 관리자
    ↓
디스크 저장 명령
    ↓
하드디스크에 기록
```

**2. 데이터 검색**
```
데이터 요청
    ↓
저장 데이터 관리자
    ↓
디스크에서 읽기
    ↓
메모리로 로드
```

**3. 버퍼 관리**
```
자주 사용하는 데이터:
- 메모리(버퍼)에 캐싱
- 디스크 I/O 최소화
- 성능 향상
```

#### 특징
```
✅ 물리적 저장 담당
✅ 운영체제와 인터페이스
✅ I/O 최적화
✅ 버퍼 관리
```

---

## 4. 데이터 처리 흐름

### 4.1 일반 사용자의 질의 처리

```
┌──────────────────────┐
│  일반 사용자         │
│  SELECT * FROM 학생; │
└──────────┬───────────┘
           ↓
┌──────────────────────┐
│  질의 처리기         │
│  - 파싱              │
│  - 분석              │
└──────────┬───────────┘
           ↓
┌──────────────────────┐
│  런타임 처리기       │
│  - 실행 준비         │
└──────────┬───────────┘
           ↓
┌──────────────────────┐
│  트랜잭션 관리자     │
│  - 권한 검사         │
│  - 무결성 검사       │
└──────────┬───────────┘
           ↓
┌──────────────────────┐
│  저장 데이터 관리자  │
│  - 디스크 읽기       │
└──────────┬───────────┘
           ↓
     [데이터베이스]
           ↓
        결과 반환
```

---

### 4.2 응용 프로그래머의 DML 처리

```
┌──────────────────────┐
│  응용 프로그램       │
│  (Java + SQL)        │
└──────────┬───────────┘
           ↓
┌──────────────────────┐
│  DML 예비 컴파일러   │
│  - Java/SQL 분리     │
└─────┬────────┬───────┘
      ↓        ↓
   [Java]   [SQL]
            ↓
┌──────────────────────┐
│  DML 컴파일러        │
│  - 파싱              │
│  - 최적화            │
└──────────┬───────────┘
           ↓
┌──────────────────────┐
│  런타임 처리기       │
│  - 실행              │
└──────────┬───────────┘
           ↓
┌──────────────────────┐
│  트랜잭션 관리자     │
│  - 제어              │
└──────────┬───────────┘
           ↓
┌──────────────────────┐
│  저장 데이터 관리자  │
│  - 저장/읽기         │
└──────────┬───────────┘
           ↓
     [데이터베이스]
```

---

### 4.3 DBA의 DDL 처리

```
┌──────────────────────┐
│  DBA                 │
│  CREATE TABLE ...    │
└──────────┬───────────┘
           ↓
┌──────────────────────┐
│  DDL 컴파일러        │
│  - 구문 분석         │
│  - 메타데이터 생성   │
└──────────┬───────────┘
           ↓
┌──────────────────────┐
│  저장 데이터 관리자  │
│  - 카탈로그 저장     │
└──────────┬───────────┘
           ↓
  [데이터 사전/카탈로그]

※ DDL은 런타임 처리나
   트랜잭션 관리를 거치지 않고
   바로 메타데이터로 저장됨
```

---

## 5. 데이터 사전 (Data Dictionary)

### 개념
메타데이터(데이터에 대한 데이터)를 저장하는 특별한 테이블입니다.

### 저장 내용

```
테이블 정보:
- 테이블명
- 컬럼명
- 데이터 타입
- 제약조건

사용자 정보:
- 사용자 계정
- 권한
- 역할

인덱스 정보:
- 인덱스명
- 인덱스 타입
- 대상 컬럼
```

### 예시

```sql
-- 데이터 사전 조회 (MySQL)
SHOW TABLES;
DESCRIBE 학생;

-- 데이터 사전 조회 (Oracle)
SELECT * FROM USER_TABLES;
SELECT * FROM USER_TAB_COLUMNS WHERE TABLE_NAME = '학생';
```

### 특징
```
✅ 자기 기술적 (Self-Descriptive)
✅ DBMS가 자동 관리
✅ 시스템 카탈로그라고도 함
✅ 읽기 전용 (일반적으로)
```

---

## 6. 전체 흐름 통합 정리

### 사용자 유형별 흐름 비교

```
[일반 사용자]
질의문 입력 → 질의 처리기 → 런타임 → 트랜잭션 관리 → 저장

[프로그래머]
프로그램 → 예비 컴파일 → DML 컴파일 → 런타임 → 트랜잭션 관리 → 저장

[DBA]
DDL 입력 → DDL 컴파일 → 저장 → 데이터 사전
```

### 모듈 간 관계도

```
        [사용자]
          ↓
    ┌─────────────┐
    │DDL/DML/Query│
    └─────────────┘
          ↓
    ┌─────────────┐
    │  컴파일러   │
    └─────────────┘
          ↓
    ┌─────────────┐
    │런타임 처리기│
    └─────────────┘
          ↓
    ┌─────────────┐
    │트랜잭션 관리│
    └─────────────┘
          ↓
    ┌─────────────┐
    │저장 관리자  │
    └─────────────┘
          ↓
    ┌─────────────┐
    │ 데이터베이스│
    │ 데이터 사전 │
    └─────────────┘
```

---

## 핵심 정리

### DBMS 3가지 사용자

```
1. 일반 사용자 → 질의어 사용
2. 응용 프로그래머 → DML 사용
3. DBA → DDL 사용
```

### DBMS 7가지 핵심 모듈

```
1. DDL 컴파일러 → 스키마 정의
2. DML 예비 컴파일러 → 언어 분리
3. DML 컴파일러 → SQL 컴파일
4. 질의 처리기 → 쿼리 파싱
5. 런타임 처리기 → 실행
6. 트랜잭션 관리자 → 제어
7. 저장 데이터 관리자 → 저장
```

### 데이터 처리 순서

```
입력 → 파싱 → 컴파일 → 실행 → 제어 → 저장 → 결과
```

### 암기 팁

```
일프디 (일반-프로그래머-DBA) → 3가지 사용자

디디질런트저데 → 7가지 모듈
(DDL, DML, 질의, 런타임, 트랜잭션, 저장, 데이터사전)
```

### 핵심 메시지

```
"DBMS는 여러 모듈이 유기적으로 연결되어
 사용자의 요청을 처리하고
 데이터를 안전하게 관리한다."
 
"각 모듈은 명확한 역할을 가지며,
 순차적으로 데이터를 처리한다."
```

---

## 마무리

이것으로 데이터베이스 구조와 DBMS의 개요에 대한 학습을 마칩니다.

### 학습한 6가지 대분류 복습

```
1. 데이터베이스의 역사와 발전
   → 파일 시스템부터 현대 빅데이터까지

2. 데이터베이스의 정의와 특성
   → 통합, 저장, 공용, 운영 데이터

3. 데이터베이스 구조
   → 개체, 속성, 관계, 논리/물리 구조

4. 파일 시스템의 문제점
   → 종속성과 중복성

5. DBMS
   → 정의, 역할, 3대 기능, 장단점

6. DBMS 구성요소
   → 사용자 유형, 내부 모듈, 처리 흐름
```


수고하셨습니다! 🎉